# 第 9 章 计算机视觉进阶任务 练习题集

## 1. 选择题：边界框

题目：结合来源「目标定位」（标题线索：目标定位；特征点检测；目标检测；基于卷积的滑动窗口实现），完成关于「边界框」的分析：它解决什么问题、核心机制是什么、如何在实验或代码中验证？

答案：边界框用坐标描述目标在图像中的位置，是目标检测任务在分类之外新增的定位输出。 本题可使用公式 `b\_x` 辅助解释。

解析：本题的来源证据是：## 目标定位 定位分类问题不仅要求判断出图片中物体的种类，还要在图片中标记出它的具体位置，用**边框（Bounding Box，或者称包围盒）**把物体圈起来。。能区分左上右下坐标和中心点宽高两种框表示。 正确答案还应从「目标定位」中提取一个证据点，例如标题层级、公式、结构、代码目的或实验现象。

常见错误：只预测类别，不评价定位框是否覆盖目标。

## 2. 判断题：IoU

题目：结合来源「第 9 章 计算机视觉进阶任务」，完成关于「IoU」的分析：它解决什么问题、核心机制是什么、如何在实验或代码中验证？

答案：IoU 用预测框与真实框交并比衡量定位质量，是检测任务筛选预测和计算指标的重要基础。 本题可使用公式 `IoU = area(intersection) / area(union)` 辅助解释。

解析：本题的来源证据是：本题主要依据课程地图、章节讲义和实验任务综合作答。。能根据两个矩形框计算交集、并集和 IoU。 正确答案还应从「第 9 章 计算机视觉进阶任务」中提取一个证据点，例如标题层级、公式、结构、代码目的或实验现象。

常见错误：只看分类置信度，不检查预测框和真实框重合程度。

## 3. 简答题：锚框

题目：结合来源「锚框」，完成关于「锚框」的分析：它解决什么问题、核心机制是什么、如何在实验或代码中验证？

答案：锚框是在图像不同位置预设的候选框，用于让检测模型同时预测多个尺度和长宽比的目标。 

解析：本题的来源证据是：# 1. 锚框 # 2. 总结 # 1. 锚框 ① 锚框的宽度和高度分别是$ws\sqrt{r}$和$hs/\sqrt{r}$。。能说明为什么一张图像同一位置可能需要多个不同长宽比锚框。 正确答案还应从「锚框」中提取一个证据点，例如标题层级、公式、结构、代码目的或实验现象。

常见错误：把锚框理解成最终预测框，而不理解还需要分类和边框回归。

## 4. 计算/推导题：R-CNN

题目：结合来源「物体检测算法R-CNN、SSD、YOLO」，完成关于「R-CNN」的分析：它解决什么问题、核心机制是什么、如何在实验或代码中验证？

答案：R-CNN 系列先产生候选区域，再对区域进行特征提取、分类和框回归，代表两阶段目标检测思想。 

解析：本题的来源证据是：# 1. RCNN # 2. SSD # 3. YOLO。能说明 region proposal 和分类器在两阶段检测中的角色。 正确答案还应从「物体检测算法R-CNN、SSD、YOLO」中提取一个证据点，例如标题层级、公式、结构、代码目的或实验现象。

常见错误：把 R-CNN 和 YOLO 都说成检测模型，却不区分两阶段和单阶段流程。

## 5. 代码理解题：SSD

题目：结合来源「物体检测算法R-CNN、SSD、YOLO」，完成关于「SSD」的分析：它解决什么问题、核心机制是什么、如何在实验或代码中验证？

答案：SSD 在多个特征层上直接预测类别和边框偏移，是典型单阶段检测方法，兼顾速度和多尺度目标。 

解析：本题的来源证据是：# 1. RCNN # 2. SSD # 3. YOLO。能解释为什么 SSD 会在不同尺度特征图上放置默认框。 正确答案还应从「物体检测算法R-CNN、SSD、YOLO」中提取一个证据点，例如标题层级、公式、结构、代码目的或实验现象。

常见错误：只记 SSD 速度快，不知道多尺度预测解决什么问题。

## 6. 实验分析题：YOLO

题目：结合来源「物体检测算法R-CNN、SSD、YOLO」，完成关于「YOLO」的分析：它解决什么问题、核心机制是什么、如何在实验或代码中验证？

答案：YOLO 将目标检测看作一次前向传播中的网格化预测，直接输出类别、置信度和边界框，强调实时性。 

解析：本题的来源证据是：# 1. RCNN # 2. SSD # 3. YOLO。能说明 YOLO 中网格单元负责预测目标的基本思想。 正确答案还应从「物体检测算法R-CNN、SSD、YOLO」中提取一个证据点，例如标题层级、公式、结构、代码目的或实验现象。

常见错误：认为 YOLO 只做分类，不理解它同时回归位置。

## 7. 选择题：语义分割

题目：结合来源「语义分割和数据集」，完成关于「语义分割」的分析：它解决什么问题、核心机制是什么、如何在实验或代码中验证？

答案：语义分割为图像中每个像素分配类别标签，相比检测框提供更细粒度的空间理解。 

解析：本题的来源证据是：# 1. 语义分割 # 2. 语义分割 ① 最重要的语义分割数据集之一是http://host.robots.ox.ac.uk/pascal/VOC/voc2012/。能区分图像分类、目标检测和语义分割的输出形式。 正确答案还应从「语义分割和数据集」中提取一个证据点，例如标题层级、公式、结构、代码目的或实验现象。

常见错误：把分割结果当作一组边界框，而不是像素级类别图。

## 8. 判断题：FCN

题目：结合来源「全连接卷积神经网络FCN」，完成关于「FCN」的分析：它解决什么问题、核心机制是什么、如何在实验或代码中验证？

答案：FCN 用全卷积结构替代全连接分类头，使网络能够输出空间尺寸对应的像素级预测。 

解析：本题的来源证据是：# 1. 全连接卷积神经网络FCN ① 1 * 1 卷积层来降低维度。。能说明上采样或转置卷积在恢复空间分辨率中的作用。 正确答案还应从「全连接卷积神经网络FCN」中提取一个证据点，例如标题层级、公式、结构、代码目的或实验现象。

常见错误：只知道 FCN 是分割网络，不理解为什么去掉全连接层。
