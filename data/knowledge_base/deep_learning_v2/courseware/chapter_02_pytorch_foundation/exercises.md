# 第 2 章 Python、NumPy 与 PyTorch 基础 练习题集

## 1. 选择题：Tensor

题目：结合来源「Tensorboard使用」，完成关于「Tensor」的分析：它解决什么问题、核心机制是什么、如何在实验或代码中验证？

答案：Tensor 是 PyTorch 中承载数据和中间结果的多维数组，图像、标签、模型输出、损失计算都围绕张量形状和数据类型展开。 

解析：本题的来源证据是：# 1. Tensorboard用途 ① Tensorboad 可以用来查看loss是否按照我们预想的变化，或者查看训练到某一步输出的图像是什么样。。能说出一批 RGB 图片进入模型前常见形状为 N×C×H×W，并解释每个维度含义。 正确答案还应从「Tensorboard使用」中提取一个证据点，例如标题层级、公式、结构、代码目的或实验现象。

常见错误：只会打印 tensor，不检查 dtype、device 和 shape 是否满足模型输入要求。

## 2. 判断题：Dataset

题目：结合来源「Pytorch加载数据」，完成关于「Dataset」的分析：它解决什么问题、核心机制是什么、如何在实验或代码中验证？

答案：Dataset 定义样本如何按索引读取，以及每个样本返回哪些内容，通常包含输入数据和标签，是数据管道的起点。 

解析：本题的来源证据是：# 1. Pytorch加载数据 ① Pytorch中加载数据需要Dataset、Dataloader。。能说明 `__len__` 和 `__getitem__` 的作用，并写出返回 image,label 的最小数据集。 正确答案还应从「Pytorch加载数据」中提取一个证据点，例如标题层级、公式、结构、代码目的或实验现象。

常见错误：把 Dataset 和 DataLoader 混在一起，分不清谁负责取单个样本、谁负责组 batch。

## 3. 简答题：DataLoader

题目：结合来源「Dataloader使用」，完成关于「DataLoader」的分析：它解决什么问题、核心机制是什么、如何在实验或代码中验证？

答案：DataLoader 负责把 Dataset 中的样本按 batch 组织起来，并处理 shuffle、batch_size、多进程加载等训练所需行为。 

解析：本题的来源证据是：# 1. Dataloader使用 ① Dataset只是去告诉我们程序，我们的数据集在什么位置，数据集第一个数据给它一个索引0，它对应的是哪一个数据。。能解释 batch_size、shuffle、num_workers 对训练流程的影响。 正确答案还应从「Dataloader使用」中提取一个证据点，例如标题层级、公式、结构、代码目的或实验现象。

常见错误：只改 batch_size，不检查最后一个 batch、标签维度和随机打乱是否合理。

## 4. 计算/推导题：Transforms

题目：结合来源「Transforms使用」，完成关于「Transforms」的分析：它解决什么问题、核心机制是什么、如何在实验或代码中验证？

答案：Transforms 是输入预处理和数据增强工具链，常用于把图片转成 Tensor、归一化、裁剪、翻转或改变尺寸。 

解析：本题的来源证据是：# 1. Transforms用途 ① Transforms当成工具箱的话，里面的class就是不同的工具。。能写出 ToTensor、Normalize、Resize/RandomCrop 在图像任务中的顺序和作用。 正确答案还应从「Transforms使用」中提取一个证据点，例如标题层级、公式、结构、代码目的或实验现象。

常见错误：训练集和测试集使用完全相同的随机增强，导致评价不稳定。

## 5. 代码理解题：nn.Module

题目：结合来源「nn.Module模块使用」，完成关于「nn.Module」的分析：它解决什么问题、核心机制是什么、如何在实验或代码中验证？

答案：nn.Module 是 PyTorch 神经网络的基类，用来组织层、参数和 forward 计算过程。自定义模型通常继承它并实现 forward。 

解析：本题的来源证据是：# 1. nn.Module模块使用 ① nn.Module是对所有神经网络提供一个基本的类。。能解释 `__init__` 中定义层、`forward` 中描述数据流的区别。 正确答案还应从「nn.Module模块使用」中提取一个证据点，例如标题层级、公式、结构、代码目的或实验现象。

常见错误：把层写在 forward 内每次重新创建，导致参数无法被优化器正确管理。

## 6. 实验分析题：训练循环

题目：结合来源「第 2 章 Python、NumPy 与 PyTorch 基础」，完成关于「训练循环」的分析：它解决什么问题、核心机制是什么、如何在实验或代码中验证？

答案：训练循环通常包含取 batch、前向计算、计算损失、清空梯度、反向传播、优化器更新和指标记录，是所有模型实验的共同骨架。 

解析：本题的来源证据是：本题主要依据课程地图、章节讲义和实验任务综合作答。。能按顺序写出 `optimizer.zero_grad()`、`loss.backward()`、`optimizer.step()` 的位置。 正确答案还应从「第 2 章 Python、NumPy 与 PyTorch 基础」中提取一个证据点，例如标题层级、公式、结构、代码目的或实验现象。

常见错误：忘记清空梯度或在验证阶段仍然更新参数。

## 7. 选择题：GPU 训练

题目：结合来源「利用GPU训练」，完成关于「GPU 训练」的分析：它解决什么问题、核心机制是什么、如何在实验或代码中验证？

答案：GPU 训练要求模型、输入张量和标签在同一 device 上，并通过 CUDA 加速张量运算；核心不是“有显卡”，而是正确迁移数据和模型。 

解析：本题的来源证据是：# 1. 利用GPU训练(方式一) ① GPU训练主要有三部分，网络模型、数据(输入、标注)、损失函数，这三部分放到GPU上。。能说明为什么 model.to(device) 后，batch 数据也需要 to(device)。 正确答案还应从「利用GPU训练」中提取一个证据点，例如标题层级、公式、结构、代码目的或实验现象。

常见错误：模型在 GPU、数据在 CPU，导致 device mismatch 报错。

## 8. 判断题：模型保存

题目：结合来源「网络模型保存与读取」，完成关于「模型保存」的分析：它解决什么问题、核心机制是什么、如何在实验或代码中验证？

答案：模型保存通常保存 state_dict 或完整模型，其中 state_dict 更适合复现实验和跨环境加载；读取时需要先构建同结构模型再加载参数。 

解析：本题的来源证据是：# 1. 网络模型保存(方式一) # 2. 网络模型导入(方式一) # 3. 网络模型保存(方式二) # 4. 网络模型导入(方式二) # 5. 网络陷阱-创建模型 # 6. 网络陷阱-失败加载模型 ① 点击 Kernel，再点击 Restart。。能区分 `torch.save(model.state_dict())` 和保存整个 model 的差异。 正确答案还应从「网络模型保存与读取」中提取一个证据点，例如标题层级、公式、结构、代码目的或实验现象。

常见错误：只保存权重，不保存模型结构、类别映射和训练配置。
