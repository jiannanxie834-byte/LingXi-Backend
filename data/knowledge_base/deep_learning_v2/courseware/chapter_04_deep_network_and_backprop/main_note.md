# 第 4 章 深层神经网络与反向传播

## 章节定位

拆解 L 层网络、前向传播、反向传播、链式法则、参数更新、梯度流和 PyTorch autograd。 本章不再使用概念占位模板，而是将 深层网络中的前向和反向传播；数学符号；损失函数与反向传播；自动求导；课程1 第4周 深层神经网络；Logistic 回归；计算机视觉 中的课程结构、公式线索和 notebook 实验顺序整理为统一讲义。学生端看到的是重构后的课程内容；原始来源只作为内部依据和可追溯证据。

## 来源融合说明

- **深层网络中的前向和反向传播**（Andrew Ng 笔记）：覆盖 深层网络中的前向和反向传播、前向传播、反向传播、搭建深层神经网络块、矩阵的维度。
  - 公式线索：a^{[l−1]}；a^{[l]}；z^{[l]}
  - 内容要点：然后再计算 $da^{[l]}$，开始实现反向传播，用**链式法则**得到所有的导数项，W 和 b 也会在每一层被更新。
  - 内容要点：在代码实现时，可以将正向传播过程中计算出来的 z 值缓存下来，待到反向传播计算时使用。
- **数学符号**（Andrew Ng 笔记）：覆盖 数学符号、循环神经网络模型、反向传播、不同结构、语言模型。
  - 公式线索：x；x^{⟨t⟩}；t
  - 内容要点：循环序列模型 自然语言和音频都是前后相互关联的数据，对于这些序列数据需要使用**循环神经网络（Recurrent Neural Network，RNN）**来进行处理。
  - 内容要点：使用 RNN 实现的应用包括下图中所示： ## 数学符号 对于一个序列数据 $x$，用符号 $x^{⟨t⟩}$来表示这个数据中的第 $t$个元素，用 $y^{⟨t⟩}$来表示第 $t$个标签，用 $T\_x$ 和 $T\_y$来表示输入和输出的长度。
- **损失函数与反向传播**（AccumulateMore/CV Notebook）：覆盖 未提取标题层级。
  - 代码线索：course code example；course code example；course code example
  - 内容要点：# 1. 损失函数 ① Loss损失函数一方面计算实际输出和目标之间的差距。
  - 内容要点：② Loss损失函数另一方面为我们更新输出提供一定的依据。
- **自动求导**（AccumulateMore/CV Notebook）：覆盖 未提取标题层级。
  - 代码线索：course code example；course code example；course code example
  - 内容要点：# 1. 向量链式法则 ① 例子1是一个线性回归的例子，如下图所示。
  - 内容要点：# 2. 自动求导 # 3. 计算图 # 4. 两种模型 ① b是之前计算的结果，是一个已知的值。
- **课程1 第4周 深层神经网络**（AccumulateMore/CV Notebook）：覆盖 未提取标题层级。
  - 内容要点：# ***深层神经网络*** # 1. 深层神经网络 ① 深层神经网络其实就是包含更多的隐藏层神经网络。
  - 内容要点：② 如下图所示，分别列举了逻辑回归、1个隐藏层的神经网络、2个隐藏层的神经网络和5个隐藏层的神经网络它们的模型结构。
- **Logistic 回归**（Andrew Ng 笔记）：覆盖 Logistic 回归、损失函数、梯度下降法（Gradient Descent）、计算图（Computation Graph）、Logistic 回归中的梯度下降法。
  - 公式线索：x \in R^{n\_x}；{n\_x}；y \in 0,1
  - 内容要点：神经网络基础 * 实现一个神经网络时，如果需要遍历整个训练集，并不需要直接使用 for 循环。
  - 内容要点：* 神经网络的计算过程中，通常有一个正向过程（forward pass）或者叫**正向传播步骤（forward propagation step）**，接着会有一个反向过程（backward pass）或者叫**反向传播步骤（backward propagation step）**。
- **计算机视觉**（Andrew Ng 笔记）：覆盖 计算机视觉、卷积运算、边缘检测、更多边缘检测的例子、填充。
  - 公式线索：n \times n；f \times f；(n-f+1) \times (n-f+1)
  - 内容要点：卷积神经网络 ## 计算机视觉 **计算机视觉（Computer Vision）**的高速发展标志着新型应用产生的可能，例如自动驾驶、人脸识别、创造新的艺术风格。
  - 内容要点：人们对于计算机视觉的研究也催生了很多机算机视觉与其他领域的交叉成果。

## 学习目标

- 能解释「L 层网络」的定义、来源中的具体语境、公式或代码位置，并完成至少一个可检查任务。
- 能解释「前向传播」的定义、来源中的具体语境、公式或代码位置，并完成至少一个可检查任务。
- 能解释「反向传播」的定义、来源中的具体语境、公式或代码位置，并完成至少一个可检查任务。
- 能解释「链式法则」的定义、来源中的具体语境、公式或代码位置，并完成至少一个可检查任务。
- 能解释「参数更新」的定义、来源中的具体语境、公式或代码位置，并完成至少一个可检查任务。
- 能解释「梯度流」的定义、来源中的具体语境、公式或代码位置，并完成至少一个可检查任务。
- 能解释「autograd」的定义、来源中的具体语境、公式或代码位置，并完成至少一个可检查任务。
- 能解释「MLP」的定义、来源中的具体语境、公式或代码位置，并完成至少一个可检查任务。

## 前置知识

学习本章前，应先确认上一章的核心对象、变量含义和实验边界。遇到公式时，不要只背符号，要能指出它在代码中对应哪一个张量、参数、损失或指标；遇到模型结构时，要能说清数据从输入到输出经过了哪些层。

## 知识结构总览

- **L 层网络**：对应来源中的「深层网络中的前向和反向传播」，学习时同时检查定义、流程、公式/代码和实验现象。
- **前向传播**：对应来源中的「深层网络中的前向和反向传播」，学习时同时检查定义、流程、公式/代码和实验现象。
- **反向传播**：对应来源中的「深层网络中的前向和反向传播」，学习时同时检查定义、流程、公式/代码和实验现象。
- **链式法则**：对应来源中的「课程综合材料」，学习时同时检查定义、流程、公式/代码和实验现象。
- **参数更新**：对应来源中的「课程综合材料」，学习时同时检查定义、流程、公式/代码和实验现象。
- **梯度流**：对应来源中的「Logistic 回归」，学习时同时检查定义、流程、公式/代码和实验现象。
- **autograd**：对应来源中的「自动求导」，学习时同时检查定义、流程、公式/代码和实验现象。
- **MLP**：对应来源中的「课程综合材料」，学习时同时检查定义、流程、公式/代码和实验现象。

## 1. L 层网络

**来源位置**：深层网络中的前向和反向传播；标题层级包括 深层网络中的前向和反向传播、前向传播、反向传播、搭建深层神经网络块、矩阵的维度、使用深层表示的原因。

**重构讲解**：L 层网络把多个线性变换和激活函数堆叠起来，逐层形成更抽象的表示；层数、宽度和激活共同决定表达能力。

**来源要点摘录**：
- 本节作为课程组织知识点，由课程地图、章节讲义和后续实验共同支撑。

**代码或实验落点**：
- 本节重点是概念、公式或结构理解；可在章节练习中补充最小实验。

**学习检查**：能写出第 l 层的 Z[l]=W[l]A[l-1]+b[l] 与 A[l]=g(Z[l])。

**常见误区**：只数层数，不检查每层输入输出维度是否匹配。

## 2. 前向传播

**来源位置**：深层网络中的前向和反向传播；标题层级包括 深层网络中的前向和反向传播、前向传播、反向传播、搭建深层神经网络块、矩阵的维度、使用深层表示的原因。

**重构讲解**：前向传播从输入开始逐层计算 Z、A 和最终预测，同时缓存中间结果供反向传播使用。

**来源要点摘录**：
- 本节作为课程组织知识点，由课程地图、章节讲义和后续实验共同支撑。

**代码或实验落点**：
- 本节重点是概念、公式或结构理解；可在章节练习中补充最小实验。

**学习检查**：能说明为什么反向传播需要前向传播中保存的 A、Z 或 mask。

**常见误区**：只关注预测结果，不保存中间变量，导致无法正确求梯度。

## 3. 反向传播

**来源位置**：深层网络中的前向和反向传播；标题层级包括 深层网络中的前向和反向传播、前向传播、反向传播、搭建深层神经网络块、矩阵的维度、使用深层表示的原因。

**公式/结构线索**：`dW[l] = dZ[l] A[l-1]^T / m`。

**重构讲解**：反向传播从损失函数出发，按链式法则逐层计算参数梯度，是深层网络能够训练的核心算法。

**来源要点摘录**：
- 然后再计算 $da^{[l]}$，开始实现反向传播，用**链式法则**得到所有的导数项，W 和 b 也会在每一层被更新。
- 在代码实现时，可以将正向传播过程中计算出来的 z 值缓存下来，待到反向传播计算时使用。

**代码或实验落点**：
- 本节重点是概念、公式或结构理解；可在章节练习中补充最小实验。

**学习检查**：能解释 dZ、dW、db、dA_prev 在一层中的含义。

**常见误区**：背梯度公式但不知道每个梯度张量的 shape。

## 4. 链式法则

**来源位置**：课程综合材料。

**公式/结构线索**：`dL/dx = dL/dy · dy/dx`。

**重构讲解**：链式法则说明复合函数的导数可以由局部导数相乘得到，反向传播正是把这个规则系统地应用到计算图。

**来源要点摘录**：
- 本节作为课程组织知识点，由课程地图、章节讲义和后续实验共同支撑。

**代码或实验落点**：
- 本节重点是概念、公式或结构理解；可在章节练习中补充最小实验。

**学习检查**：能对 y=g(f(x)) 写出 dy/dx = dy/dg · dg/df · df/dx。

**常见误区**：只看最终导数，不分析中间变量依赖关系。

## 5. 参数更新

**来源位置**：课程综合材料。

**公式/结构线索**：`theta <- theta - alpha · grad`。

**重构讲解**：参数更新根据梯度和学习率调整 W、b 或其他可学习参数，使下一次前向传播的损失有机会降低。

**来源要点摘录**：
- 本节作为课程组织知识点，由课程地图、章节讲义和后续实验共同支撑。

**代码或实验落点**：
- 本节重点是概念、公式或结构理解；可在章节练习中补充最小实验。

**学习检查**：能说明学习率过大和过小时参数更新的表现。

**常见误区**：把梯度方向和参数更新方向混淆，忘记是沿负梯度下降。

## 6. 梯度流

**来源位置**：Logistic 回归；标题层级包括 Logistic 回归、损失函数、梯度下降法（Gradient Descent）、计算图（Computation Graph）、Logistic 回归中的梯度下降法、向量化（Vectorization）。

**重构讲解**：梯度流描述误差信号在网络中反向传播的强弱和路径，过弱会导致梯度消失，过强可能导致梯度爆炸。

**来源要点摘录**：
- 最简单的损失函数定义方式为平方差损失：$$L(\hat{y},y) = \frac{1}{2}(\hat{y}-y)^2$$ 但 Logistic 回归中我们并不倾向于使用这样的损失函数，因为之后讨论的优化问题会变成非凸的，最后会得到很多个局部最优解，梯度下降法可能找不到全局最优值。
- $$J(w,b) = \frac{1}{m}\sum_{i=1}^mL(\hat{y}^{(i)},y^{(i)})$$ ## 梯度下降法（Gradient Descent） 函数的**梯度（gradient）**指出了函数的最陡增长方向。
- 那么按梯度的负方向走，函数值自然就降低得最快了。
- 在成本函数 J(w, b) 中还存在参数 b，因此也有： $$b := b - \alpha\frac{dJ(w, b)}{db}$$ ## 计算图（Computation Graph） 神经网络中的计算即是由多个计算网络输出的前向传播与计算梯度的后向

**代码或实验落点**：
- 本节重点是概念、公式或结构理解；可在章节练习中补充最小实验。

**学习检查**：能根据梯度范数或训练曲线判断深层网络训练是否异常。

**常见误区**：只看前向结构，不检查反向梯度能否有效到达早期层。

## 7. autograd

**来源位置**：自动求导。

**重构讲解**：PyTorch autograd 自动记录张量运算图，并在 backward 时计算梯度，使用户无需手写大部分反向传播公式。

**来源要点摘录**：
- # 2. 自动求导 # 3. 计算图 # 4. 两种模型 ① b是之前计算的结果，是一个已知的值。
- # 5. 复杂度 # 6. 自动求导 ① 假设想对函数$y=2\mathbf{x}^{\top}\mathbf{x}$关于列向量x求导。

**代码或实验落点**：
- course code example
- course code example
- course code example

**学习检查**：能说明 requires_grad、loss.backward 和 parameter.grad 的关系。

**常见误区**：在不该跟踪梯度的验证阶段没有使用 no_grad，浪费显存并引入误解。

## 8. MLP

**来源位置**：课程综合材料。

**重构讲解**：MLP 由多层全连接层和非线性激活构成，适合作为理解深层网络、分类器头和基础训练循环的入门模型。

**来源要点摘录**：
- 本节作为课程组织知识点，由课程地图、章节讲义和后续实验共同支撑。

**代码或实验落点**：
- 本节重点是概念、公式或结构理解；可在章节练习中补充最小实验。

**学习检查**：能写出 Flatten -> Linear -> ReLU -> Linear 的简单分类网络。

**常见误区**：直接把高维图像展平给 MLP，而不分析空间结构丢失和参数量增长。

## 教材式例子一：从来源结构到学习流程

以「深层网络中的前向和反向传播」为例，先读取来源标题层级，判断它属于概念解释、模型结构、优化策略还是实验任务；再将它映射到本章的 L 层网络, 前向传播, 反向传播, 链式法则；最后生成一条可执行学习任务：阅读主讲义、完成练习、运行或观察实验、写出错因复盘。

## 教材式例子二：从 notebook 到课程实验

AccumulateMore/CV 中的 notebook 不是直接塞给学生，而是被拆成实验目标、关键代码用途、运行步骤和调参任务。学生学习 第 4 章 深层神经网络与反向传播 时，应能把 `Dataset/DataLoader`、`nn.Module`、`loss.backward()`、`optimizer.step()` 或对应模型结构放回完整训练循环。

## 易错点与纠偏

- 只背来源标题，不知道标题下真正讨论了什么模型、公式或实验。
- 把 notebook 当成可直接展示的学习资源，而没有整理成目标、步骤、任务和评价。
- 学模型结构时不检查输入输出 shape，导致代码能抄但不能解释。
- 学优化或正则化时不看训练/验证曲线，无法判断方法是否真的改善泛化。

## 课堂讨论问题

1. L 层网络 与 前向传播 在本章流程中分别解决什么问题？
2. 如果学生已经会使用工具库，但解释不清 L 层网络，应该如何安排补弱？
3. 哪些内容适合做动画或流程图，哪些内容更适合做代码实验？

## 自测题

1. 围绕「L 层网络」写出定义、输入输出、常见误区和一个应用场景。
2. 围绕「前向传播」写出定义、输入输出、常见误区和一个应用场景。
3. 围绕「反向传播」写出定义、输入输出、常见误区和一个应用场景。
4. 围绕「链式法则」写出定义、输入输出、常见误区和一个应用场景。
5. 围绕「参数更新」写出定义、输入输出、常见误区和一个应用场景。
6. 围绕「梯度流」写出定义、输入输出、常见误区和一个应用场景。
7. 围绕「autograd」写出定义、输入输出、常见误区和一个应用场景。
8. 围绕「MLP」写出定义、输入输出、常见误区和一个应用场景。
9. 围绕「L 层网络」写出定义、输入输出、常见误区和一个应用场景。
10. 围绕「前向传播」写出定义、输入输出、常见误区和一个应用场景。
11. 围绕「反向传播」写出定义、输入输出、常见误区和一个应用场景。
12. 围绕「链式法则」写出定义、输入输出、常见误区和一个应用场景。

## 小结与下一步

完成本章后，应能把 L 层网络, 前向传播, 反向传播, 链式法则 串成一条可执行的学习路径。下一步不是盲目堆资料，而是根据画像、诊断结果和当前章节位置选择主讲义、练习、实验或项目任务。
