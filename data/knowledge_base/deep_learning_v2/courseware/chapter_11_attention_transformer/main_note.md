# 第 11 章 Attention、Transformer 与 NLP 基础

## 章节定位

从词嵌入、语言模型、Encoder-Decoder、Q/K/V、缩放点积注意力、多头注意力到 Transformer 与 BERT 拓展。 本章不再使用概念占位模板，而是将 Seq2Seq 模型；使用注意力机制的seq2seq；词嵌入；序列到序列学习seq2seq；注意力机制；注意力分数；自注意力；Transformer 中的课程结构、公式线索和 notebook 实验顺序整理为统一讲义。学生端看到的是重构后的课程内容；原始来源只作为内部依据和可追溯证据。

## 来源融合说明

- **Seq2Seq 模型**（Andrew Ng 笔记）：覆盖 Seq2Seq 模型、选择最可能的句子、集束搜索、优化：长度标准化、误差分析。
  - 公式线索：arg \ max\_{y^{⟨1⟩}, ..., y^{⟨T\_y⟩}}P(y^{⟨1⟩}, ..., y^{⟨T\_y⟩} | x)；B；B=3
  - 内容要点：序列模型和注意力机制 ## Seq2Seq 模型 **Seq2Seq（Sequence-to-Sequence）**模型能够应用于机器翻译、语音识别等各种序列到序列的转换问题。
  - 内容要点：一个 Seq2Seq 模型包含**编码器（Encoder）**和**解码器（Decoder）**两部分，它们通常是两个不同的 RNN。
- **使用注意力机制的seq2seq**（AccumulateMore/CV Notebook）：覆盖 未提取标题层级。
  - 代码线索：course code example；PyTorch model definition；PyTorch model definition
  - 内容要点：# 1. 使用注意力机制的seq2seq
- **词嵌入**（Andrew Ng 笔记）：覆盖 词嵌入、词嵌入与迁移学习、词嵌入与类比推理、嵌入矩阵、学习词嵌入。
  - 公式线索：e\_{man} - e\_{woman} \approx e\_{king} - e\_?；w；sim(e\_w, e\_{king} - e\_{man} + e\_{woman})
  - 内容要点：自然语言处理与词嵌入 ## 词嵌入 one-hot 向量将每个单词表示为完全独立的个体，不同词向量都是正交的，因此单词间的相似度无法体现。
  - 内容要点：我们可以通过用语义特征作为维度来表示一个词，因此语义相近的词，其词向量也相近。
- **序列到序列学习seq2seq**（AccumulateMore/CV Notebook）：覆盖 未提取标题层级。
  - 代码线索：course code example；PyTorch model definition；course code example
  - 内容要点：# 1. 序列到序列学习(seq2seq) # 2. 序列到序列学习(seq2seq)
- **注意力机制**（AccumulateMore/CV Notebook）：覆盖 未提取标题层级。
  - 代码线索：course code example；course code example；course code example
  - 内容要点：# 1. 注意力机制
- **注意力分数**（AccumulateMore/CV Notebook）：覆盖 未提取标题层级。
  - 代码线索：course code example；course code example；course code example
  - 内容要点：# 1. 注意力分数
- **自注意力**（AccumulateMore/CV Notebook）：覆盖 未提取标题层级。
  - 代码线索：course code example；attention or transformer example；attention or transformer example
  - 内容要点：# 1. 自注意力 # 2. 自注意力
- **Transformer**（AccumulateMore/CV Notebook）：覆盖 未提取标题层级。
  - 代码线索：course code example；PyTorch model definition；course code example
  - 内容要点：# 1. Transformer # 2. Transformer

## 学习目标

- 能解释「词嵌入」的定义、来源中的具体语境、公式或代码位置，并完成至少一个可检查任务。
- 能解释「语言模型」的定义、来源中的具体语境、公式或代码位置，并完成至少一个可检查任务。
- 能解释「Encoder-Decoder」的定义、来源中的具体语境、公式或代码位置，并完成至少一个可检查任务。
- 能解释「Attention」的定义、来源中的具体语境、公式或代码位置，并完成至少一个可检查任务。
- 能解释「Q/K/V」的定义、来源中的具体语境、公式或代码位置，并完成至少一个可检查任务。
- 能解释「Scaled Dot-Product」的定义、来源中的具体语境、公式或代码位置，并完成至少一个可检查任务。
- 能解释「Multi-Head」的定义、来源中的具体语境、公式或代码位置，并完成至少一个可检查任务。
- 能解释「Positional Encoding」的定义、来源中的具体语境、公式或代码位置，并完成至少一个可检查任务。

## 前置知识

学习本章前，应先确认上一章的核心对象、变量含义和实验边界。遇到公式时，不要只背符号，要能指出它在代码中对应哪一个张量、参数、损失或指标；遇到模型结构时，要能说清数据从输入到输出经过了哪些层。

## 知识结构总览

- **词嵌入**：对应来源中的「词嵌入」，学习时同时检查定义、流程、公式/代码和实验现象。
- **语言模型**：对应来源中的「词嵌入」，学习时同时检查定义、流程、公式/代码和实验现象。
- **Encoder-Decoder**：对应来源中的「Seq2Seq 模型」，学习时同时检查定义、流程、公式/代码和实验现象。
- **Attention**：对应来源中的「课程综合材料」，学习时同时检查定义、流程、公式/代码和实验现象。
- **Q/K/V**：对应来源中的「课程综合材料」，学习时同时检查定义、流程、公式/代码和实验现象。
- **Scaled Dot-Product**：对应来源中的「课程综合材料」，学习时同时检查定义、流程、公式/代码和实验现象。
- **Multi-Head**：对应来源中的「课程综合材料」，学习时同时检查定义、流程、公式/代码和实验现象。
- **Positional Encoding**：对应来源中的「课程综合材料」，学习时同时检查定义、流程、公式/代码和实验现象。
- **Transformer**：对应来源中的「Transformer」，学习时同时检查定义、流程、公式/代码和实验现象。
- **BERT**：对应来源中的「BERT预训练」，学习时同时检查定义、流程、公式/代码和实验现象。

## 1. 词嵌入

**来源位置**：词嵌入；标题层级包括 词嵌入、词嵌入与迁移学习、词嵌入与类比推理、嵌入矩阵、学习词嵌入、神经概率语言模型。

**重构讲解**：词嵌入把离散词转换为连续向量，使语义相近的词在向量空间中更接近，是 NLP 深度模型的基础表示。

**来源要点摘录**：
- 自然语言处理与词嵌入 ## 词嵌入 one-hot 向量将每个单词表示为完全独立的个体，不同词向量都是正交的，因此单词间的相似度无法体现。
- 将高维的词嵌入“嵌入”到一个二维空间里，就可以进行可视化。
- 对大量词汇进行词嵌入后获得的词向量，可用于完成**命名实体识别（Named Entity Recognition）**等任务。
- ### 词嵌入与迁移学习 用词嵌入做迁移学习可以降低学习成本，提高效率。

**代码或实验落点**：
- 本节重点是概念、公式或结构理解；可在章节练习中补充最小实验。

**学习检查**：能说明 one-hot 和 embedding 在维度、相似度表达上的差异。

**常见误区**：把词嵌入当固定编号，不理解向量会参与训练或来自预训练。

## 2. 语言模型

**来源位置**：词嵌入；标题层级包括 词嵌入、词嵌入与迁移学习、词嵌入与类比推理、嵌入矩阵、学习词嵌入、神经概率语言模型。

**重构讲解**：语言模型根据上下文预测下一个词或序列概率，是文本生成、表示学习和预训练模型的重要基础。

**来源要点摘录**：
- ## 学习词嵌入 ### 神经概率语言模型 **神经概率语言模型（Neural Probabilistic Language Model）**构建了一个能够通过上下文来预测未知词的神经网络，在训练这个语言模型的同时学习词嵌入。
- 相关论文：Bengio et. al., 2003, A neural probabilistic language model ###

**代码或实验落点**：
- 本节重点是概念、公式或结构理解；可在章节练习中补充最小实验。

**学习检查**：能说明 P(w_t | w_1...w_{t-1}) 的含义。

**常见误区**：只看生成结果流畅度，不检查上下文条件和训练目标。

## 3. Encoder-Decoder

**来源位置**：Seq2Seq 模型；标题层级包括 Seq2Seq 模型、选择最可能的句子、集束搜索、优化：长度标准化、误差分析、Bleu 得分。

**重构讲解**：Encoder-Decoder 先把输入序列编码成中间表示，再由解码器生成目标序列，常用于翻译、摘要等序列到序列任务。

**来源要点摘录**：
- 一个 Seq2Seq 模型包含**编码器（Encoder）**和**解码器（Decoder）**两部分，它们通常是两个不同的 RNN。
- 如下图所示，将编码器的输出作为解码器的输入，由解码器负责输出正确的翻译结果。
- 将 AlexNet 作为编码器，最后一层的 Softmax 换成一个 RNN 作为解码器，网络的输出序列就是对图像的一个描述。
- 由于解码器进行随机采样过程，输出的翻译结果可能有好有坏。

**代码或实验落点**：
- 本节重点是概念、公式或结构理解；可在章节练习中补充最小实验。

**学习检查**：能说明 encoder hidden states 和 decoder outputs 的关系。

**常见误区**：把编码器输出当单个固定向量，忽略注意力机制对不同位置的访问。

## 4. Attention

**来源位置**：课程综合材料。

**重构讲解**：Attention 根据查询和键的匹配程度为不同值分配权重，使模型能在生成或编码当前表示时关注相关位置。

**来源要点摘录**：
- 本节作为课程组织知识点，由课程地图、章节讲义和后续实验共同支撑。

**代码或实验落点**：
- 本节重点是概念、公式或结构理解；可在章节练习中补充最小实验。

**学习检查**：能解释 Q、K、V 的来源和注意力权重如何影响输出。

**常见误区**：把注意力权重直接当成绝对解释，而不检查模型上下文。

## 5. Q/K/V

**来源位置**：课程综合材料。

**重构讲解**：Q/K/V 分别表示查询、键和值，注意力通过 Q 与 K 的匹配计算权重，再对 V 加权求和得到上下文表示。

**来源要点摘录**：
- 本节作为课程组织知识点，由课程地图、章节讲义和后续实验共同支撑。

**代码或实验落点**：
- 本节重点是概念、公式或结构理解；可在章节练习中补充最小实验。

**学习检查**：能用“问题检索资料”的类比解释 Q、K、V 的作用。

**常见误区**：只背三个字母，不知道权重来自 QK 相似度。

## 6. Scaled Dot-Product

**来源位置**：课程综合材料。

**重构讲解**：Scaled Dot-Product Attention 用 QK^T 衡量相关性，再除以 sqrt(d_k) 控制数值尺度，最后 softmax 得到注意力权重。

**来源要点摘录**：
- 本节作为课程组织知识点，由课程地图、章节讲义和后续实验共同支撑。

**代码或实验落点**：
- 本节重点是概念、公式或结构理解；可在章节练习中补充最小实验。

**学习检查**：能说明为什么维度较大时需要缩放。

**常见误区**：漏掉缩放项，导致 softmax 过于尖锐、梯度不稳定。

## 7. Multi-Head

**来源位置**：课程综合材料。

**重构讲解**：多头注意力把表示投影到多个子空间并并行计算注意力，使模型能从不同关系角度观察序列。

**来源要点摘录**：
- 本节作为课程组织知识点，由课程地图、章节讲义和后续实验共同支撑。

**代码或实验落点**：
- 本节重点是概念、公式或结构理解；可在章节练习中补充最小实验。

**学习检查**：能解释多个 head 为什么不是简单重复同一注意力。

**常见误区**：只增加 head 数量，不考虑 hidden size、计算量和任务规模。

## 8. Positional Encoding

**来源位置**：课程综合材料。

**重构讲解**：位置编码向 Transformer 输入注入顺序信息，因为纯自注意力本身不区分 token 的先后位置。

**来源要点摘录**：
- 本节作为课程组织知识点，由课程地图、章节讲义和后续实验共同支撑。

**代码或实验落点**：
- 本节重点是概念、公式或结构理解；可在章节练习中补充最小实验。

**学习检查**：能说明如果没有位置编码，句子顺序信息会怎样丢失。

**常见误区**：认为 Transformer 天然知道顺序，而忽略位置表示。

## 9. Transformer

**来源位置**：Transformer。

**重构讲解**：Transformer 用自注意力和前馈网络替代循环结构，并通过多头注意力和位置编码建模序列内部依赖，是现代 NLP 与多模态模型的重要基础。

**来源要点摘录**：
- # 1. Transformer # 2. Transformer

**代码或实验落点**：
- course code example
- PyTorch model definition
- course code example

**学习检查**：能画出 Encoder block 中 Multi-Head Attention、AddNorm、FFN 的顺序。

**常见误区**：只记 Transformer 很强，不知道它为什么能并行处理序列。

## 10. BERT

**来源位置**：BERT预训练。

**重构讲解**：BERT 使用 Transformer Encoder 和预训练任务学习双向上下文表示，再通过微调用于分类、问答等下游任务。

**来源要点摘录**：
- # 1. BERT # 2. BERT # 3. BERT预训练数据集 # 4. 预训练BERT

**代码或实验落点**：
- course code example
- course code example
- PyTorch model definition

**学习检查**：能说明预训练和微调在数据、目标和参数更新上的区别。

**常见误区**：把 BERT 当普通词向量，不理解它输出的是上下文化表示。

## 教材式例子一：从来源结构到学习流程

以「Seq2Seq 模型」为例，先读取来源标题层级，判断它属于概念解释、模型结构、优化策略还是实验任务；再将它映射到本章的 词嵌入, 语言模型, Encoder-Decoder, Attention；最后生成一条可执行学习任务：阅读主讲义、完成练习、运行或观察实验、写出错因复盘。

## 教材式例子二：从 notebook 到课程实验

AccumulateMore/CV 中的 notebook 不是直接塞给学生，而是被拆成实验目标、关键代码用途、运行步骤和调参任务。学生学习 第 11 章 Attention、Transformer 与 NLP 基础 时，应能把 `Dataset/DataLoader`、`nn.Module`、`loss.backward()`、`optimizer.step()` 或对应模型结构放回完整训练循环。

## 易错点与纠偏

- 只背来源标题，不知道标题下真正讨论了什么模型、公式或实验。
- 把 notebook 当成可直接展示的学习资源，而没有整理成目标、步骤、任务和评价。
- 学模型结构时不检查输入输出 shape，导致代码能抄但不能解释。
- 学优化或正则化时不看训练/验证曲线，无法判断方法是否真的改善泛化。

## 课堂讨论问题

1. 词嵌入 与 语言模型 在本章流程中分别解决什么问题？
2. 如果学生已经会使用工具库，但解释不清 词嵌入，应该如何安排补弱？
3. 哪些内容适合做动画或流程图，哪些内容更适合做代码实验？

## 自测题

1. 围绕「词嵌入」写出定义、输入输出、常见误区和一个应用场景。
2. 围绕「语言模型」写出定义、输入输出、常见误区和一个应用场景。
3. 围绕「Encoder-Decoder」写出定义、输入输出、常见误区和一个应用场景。
4. 围绕「Attention」写出定义、输入输出、常见误区和一个应用场景。
5. 围绕「Q/K/V」写出定义、输入输出、常见误区和一个应用场景。
6. 围绕「Scaled Dot-Product」写出定义、输入输出、常见误区和一个应用场景。
7. 围绕「Multi-Head」写出定义、输入输出、常见误区和一个应用场景。
8. 围绕「Positional Encoding」写出定义、输入输出、常见误区和一个应用场景。
9. 围绕「Transformer」写出定义、输入输出、常见误区和一个应用场景。
10. 围绕「BERT」写出定义、输入输出、常见误区和一个应用场景。
11. 围绕「词嵌入」写出定义、输入输出、常见误区和一个应用场景。
12. 围绕「语言模型」写出定义、输入输出、常见误区和一个应用场景。

## 小结与下一步

完成本章后，应能把 词嵌入, 语言模型, Encoder-Decoder, Attention 串成一条可执行的学习路径。下一步不是盲目堆资料，而是根据画像、诊断结果和当前章节位置选择主讲义、练习、实验或项目任务。
