# 第 3 章 神经网络基础与向量化计算

## 章节定位

从逻辑回归、损失函数、计算图、梯度下降、向量化和浅层神经网络进入神经网络建模。 本章不再使用概念占位模板，而是将 Logistic 回归；课程1 第2周b 神经网络基础之Python与向量化；损失函数与反向传播；Softmax回归、损失函数、分类；课程1 第2周a 神经网络基础之逻辑回归；课程1 第3周 浅层神经网络；batch 梯度下降法；深层网络中的前向和反向传播 中的课程结构、公式线索和 notebook 实验顺序整理为统一讲义。学生端看到的是重构后的课程内容；原始来源只作为内部依据和可追溯证据。

## 来源融合说明

- **Logistic 回归**（Andrew Ng 笔记）：覆盖 Logistic 回归、损失函数、梯度下降法（Gradient Descent）、计算图（Computation Graph）、Logistic 回归中的梯度下降法。
  - 公式线索：x \in R^{n\_x}；{n\_x}；y \in 0,1
  - 内容要点：神经网络基础 * 实现一个神经网络时，如果需要遍历整个训练集，并不需要直接使用 for 循环。
  - 内容要点：* 神经网络的计算过程中，通常有一个正向过程（forward pass）或者叫**正向传播步骤（forward propagation step）**，接着会有一个反向过程（backward pass）或者叫**反向传播步骤（backward propagation step）**。
- **课程1 第2周b 神经网络基础之Python与向量化**（AccumulateMore/CV Notebook）：覆盖 未提取标题层级。
  - 内容要点：# ***神经网络基础之Python与向量化*** # 1. 向量化 ① 深度学习算法中，数据量很大，在程序中应该尽量减少使用loop循环语句，而可以使用向量运算来提高程序运行速度。
  - 内容要点：② 向量化（Vectorization）就是利用矩阵运算的思想，大大提高运算速度。
- **损失函数与反向传播**（AccumulateMore/CV Notebook）：覆盖 未提取标题层级。
  - 代码线索：course code example；course code example；course code example
  - 内容要点：# 1. 损失函数 ① Loss损失函数一方面计算实际输出和目标之间的差距。
  - 内容要点：② Loss损失函数另一方面为我们更新输出提供一定的依据。
- **Softmax回归、损失函数、分类**（AccumulateMore/CV Notebook）：覆盖 未提取标题层级。
  - 代码线索：course code example；course code example；course code example
  - 内容要点：# 1. Softmax回归 ① Softmax回归虽然它的名字是回归，其实它是一个分类问题。
  - 内容要点：# 2. 回归VS分类 # 3. Kaggle分类问题 # 4. 回归到分类 # 5. 交叉熵损失 # 6. 总结 # 7. 损失函数 ① 三个常用的损失函数 L2 loss、L1 loss、Huber's Robust loss。
- **课程1 第2周a 神经网络基础之逻辑回归**（AccumulateMore/CV Notebook）：覆盖 未提取标题层级。
  - 代码线索：course code example
  - 内容要点：# ***神经网络基础之逻辑回归*** # 1. 二分类 ① 我们知道逻辑回归模型一般用来解决二分类（Binary Classification）问题。
  - 内容要点：② 二分类就是输出y只有{0,1}两个离散值（也有{-1,1}的情况）。
- **课程1 第3周 浅层神经网络**（AccumulateMore/CV Notebook）：覆盖 未提取标题层级。
  - 内容要点：# ***浅层神经网络*** # 1. 神经网络概述 ① 前面的课程中，我们已经使用计算图的方式介绍了逻辑回归梯度下降算法的正向传播和反向传播两个过程。
  - 内容要点：② 神经网络的结构与逻辑回归类似，只是神经网络的层数比逻辑回归多一层，多出来的中间那层称为隐藏层或中间层。
- **batch 梯度下降法**（Andrew Ng 笔记）：覆盖 batch 梯度下降法、Mini-Batch 梯度下降法、batch 的不同大小（size）带来的影响、mini-batch 大小的选择、获得 mini-batch 的步骤。
  - 公式线索：2^6；2^7；2^9
  - 内容要点：优化算法 深度学习难以在大数据领域发挥最大效果的一个原因是，在巨大的数据集基础上进行训练速度很慢。
  - 内容要点：## batch 梯度下降法 **batch 梯度下降法**（批梯度下降法，我们之前一直使用的梯度下降法）是最常用的梯度下降形式，即同时处理整个训练集。
- **深层网络中的前向和反向传播**（Andrew Ng 笔记）：覆盖 深层网络中的前向和反向传播、前向传播、反向传播、搭建深层神经网络块、矩阵的维度。
  - 公式线索：a^{[l−1]}；a^{[l]}；z^{[l]}
  - 内容要点：然后再计算 $da^{[l]}$，开始实现反向传播，用**链式法则**得到所有的导数项，W 和 b 也会在每一层被更新。
  - 内容要点：在代码实现时，可以将正向传播过程中计算出来的 z 值缓存下来，待到反向传播计算时使用。

## 学习目标

- 能解释「Logistic Regression」的定义、来源中的具体语境、公式或代码位置，并完成至少一个可检查任务。
- 能解释「损失函数」的定义、来源中的具体语境、公式或代码位置，并完成至少一个可检查任务。
- 能解释「代价函数」的定义、来源中的具体语境、公式或代码位置，并完成至少一个可检查任务。
- 能解释「计算图」的定义、来源中的具体语境、公式或代码位置，并完成至少一个可检查任务。
- 能解释「向量化」的定义、来源中的具体语境、公式或代码位置，并完成至少一个可检查任务。
- 能解释「广播机制」的定义、来源中的具体语境、公式或代码位置，并完成至少一个可检查任务。
- 能解释「浅层神经网络」的定义、来源中的具体语境、公式或代码位置，并完成至少一个可检查任务。
- 能解释「激活函数」的定义、来源中的具体语境、公式或代码位置，并完成至少一个可检查任务。

## 前置知识

学习本章前，应先确认上一章的核心对象、变量含义和实验边界。遇到公式时，不要只背符号，要能指出它在代码中对应哪一个张量、参数、损失或指标；遇到模型结构时，要能说清数据从输入到输出经过了哪些层。

## 知识结构总览

- **Logistic Regression**：对应来源中的「Logistic 回归」，学习时同时检查定义、流程、公式/代码和实验现象。
- **损失函数**：对应来源中的「Logistic 回归」，学习时同时检查定义、流程、公式/代码和实验现象。
- **代价函数**：对应来源中的「Logistic 回归」，学习时同时检查定义、流程、公式/代码和实验现象。
- **计算图**：对应来源中的「Logistic 回归」，学习时同时检查定义、流程、公式/代码和实验现象。
- **向量化**：对应来源中的「Logistic 回归」，学习时同时检查定义、流程、公式/代码和实验现象。
- **广播机制**：对应来源中的「Logistic 回归」，学习时同时检查定义、流程、公式/代码和实验现象。
- **浅层神经网络**：对应来源中的「课程1 第3周 浅层神经网络」，学习时同时检查定义、流程、公式/代码和实验现象。
- **激活函数**：对应来源中的「神经网络表示」，学习时同时检查定义、流程、公式/代码和实验现象。

## 1. Logistic Regression

**来源位置**：Logistic 回归；标题层级包括 Logistic 回归、损失函数、梯度下降法（Gradient Descent）、计算图（Computation Graph）、Logistic 回归中的梯度下降法、向量化（Vectorization）。

**公式/结构线索**：`a = sigmoid(w^T x + b)`。

**重构讲解**：Logistic Regression 是二分类神经网络的入口案例，通过线性变换和 sigmoid 输出正类概率，是理解损失函数、梯度和向量化的基础。

**来源要点摘录**：
- ## Logistic 回归 Logistic 回归是一个用于二分分类的算法。
- Logistic 回归中使用的参数如下： * 输入的特征向量：$x \in R^{n\_x}$，其中 ${n\_x}$是特征数量；
- Logistic 回归可以看作是一个非常小的神经网络。
- 最简单的损失函数定义方式为平方差损失：$$L(\hat{y},y) = \frac{1}{2}(\hat{y}-y)^2$$ 但 Logistic 回归中我们并不倾向于使用这样的损失函数，因为之后讨论的优化问题会变成非凸的，最后会得到很多个局部最优解，梯度下降法可能找不到全局最优值。

**代码或实验落点**：
- 本节重点是概念、公式或结构理解；可在章节练习中补充最小实验。

**学习检查**：能写出 z=w^Tx+b、a=sigmoid(z)，并说明 a 为什么可以看作概率。

**常见误区**：把 Logistic Regression 当普通线性回归，忽略 sigmoid 和交叉熵损失。

## 2. 损失函数

**来源位置**：Logistic 回归；标题层级包括 Logistic 回归、损失函数、梯度下降法（Gradient Descent）、计算图（Computation Graph）、Logistic 回归中的梯度下降法、向量化（Vectorization）。

**重构讲解**：损失函数衡量单个样本预测与真实标签之间的差异，深度学习通过让损失变小来推动参数学习。

**来源要点摘录**：
- 下图是一个典型例子： ### 损失函数 **损失函数（loss function）**用于衡量预测结果与真实值之间的误差。
- 最简单的损失函数定义方式为平方差损失：$$L(\hat{y},y) = \frac{1}{2}(\hat{y}-y)^2$$ 但 Logistic 回归中我们并不倾向于使用这样的损失函数，因为之后讨论的优化问题会变成非凸的，最后会得到很多个局部最优解，梯度下降法可能找不到全局最优值。
- 一般使用$$L(\hat{y},y) = -(y\log\hat{y})-(1-y)\log(1-\hat{y})$$ 损失函数是在单个训练样本中定义的，它衡量了在**单个**训练样本上的表现。

**代码或实验落点**：
- 本节重点是概念、公式或结构理解；可在章节练习中补充最小实验。

**学习检查**：能说明二分类交叉熵为什么比平方误差更适合 Logistic Regression。

**常见误区**：只看 accuracy，不分析 loss 是否正常下降。

## 3. 代价函数

**来源位置**：Logistic 回归；标题层级包括 Logistic 回归、损失函数、梯度下降法（Gradient Descent）、计算图（Computation Graph）、Logistic 回归中的梯度下降法、向量化（Vectorization）。

**重构讲解**：代价函数通常是训练集上所有样本损失的平均值，也可以加入正则项，用来定义整体优化目标。

**来源要点摘录**：
- 而**代价函数（cost function，或者称作成本函数）**衡量的是在**全体**训练样本上的表现，即衡量参数 w 和 b 的效果。
- 模型的训练目标即是寻找合适的 w 与 b 以最小化代价函数值。
- 简单起见我们先假设 w 与 b 都是一维实数，那么可以得到如下的 J 关于 w 与 b 的图： 可以看到，成本函数 J 是一个**凸函数**，与非凸函数的区别在于其不含有多个局部最低点；选择这样的代价函数就保证了无论我们初始化模型参数如何，都能够寻找到合适的最优解。

**代码或实验落点**：
- 本节重点是概念、公式或结构理解；可在章节练习中补充最小实验。

**学习检查**：能区分单样本 loss 和全训练集 cost 的关系。

**常见误区**：把单个样本损失和整体训练目标混用。

## 4. 计算图

**来源位置**：Logistic 回归；标题层级包括 Logistic 回归、损失函数、梯度下降法（Gradient Descent）、计算图（Computation Graph）、Logistic 回归中的梯度下降法、向量化（Vectorization）。

**重构讲解**：计算图把复杂函数拆成一系列节点，前向传播计算输出，反向传播沿图应用链式法则求梯度。

**来源要点摘录**：
- 在成本函数 J(w, b) 中还存在参数 b，因此也有： $$b := b - \alpha\frac{dJ(w, b)}{db}$$ ## 计算图（Computation Graph） 神经网络中的计算即是由多个计算网络输出的前向传播与计算梯度的后向

**代码或实验落点**：
- 本节重点是概念、公式或结构理解；可在章节练习中补充最小实验。

**学习检查**：能以 J(a,b,c)=3(a+bc) 为例写出中间变量和梯度传播顺序。

**常见误区**：只记最终公式，不知道梯度如何沿中间变量传回参数。

## 5. 向量化

**来源位置**：Logistic 回归；标题层级包括 Logistic 回归、损失函数、梯度下降法（Gradient Descent）、计算图（Computation Graph）、Logistic 回归中的梯度下降法、向量化（Vectorization）。

**公式/结构线索**：`Z = W X + b`。

**重构讲解**：向量化用矩阵运算替代显式 for 循环，让多个样本或多个神经元同时计算，是深度学习训练高效的关键。

**来源要点摘录**：
- 本节作为课程组织知识点，由课程地图、章节讲义和后续实验共同支撑。

**代码或实验落点**：
- 本节重点是概念、公式或结构理解；可在章节练习中补充最小实验。

**学习检查**：能把逐样本计算改写成 Z=WX+b，并说明 X 的列/行如何组织样本。

**常见误区**：为了能运行随意 reshape，导致样本维度和特征维度被混淆。

## 6. 广播机制

**来源位置**：Logistic 回归；标题层级包括 Logistic 回归、损失函数、梯度下降法（Gradient Descent）、计算图（Computation Graph）、Logistic 回归中的梯度下降法、向量化（Vectorization）。

**重构讲解**：广播机制允许不同形状的数组在满足规则时自动扩展，例如给矩阵每一列加同一个偏置向量。

**来源要点摘录**：
- 本节作为课程组织知识点，由课程地图、章节讲义和后续实验共同支撑。

**代码或实验落点**：
- 本节重点是概念、公式或结构理解；可在章节练习中补充最小实验。

**学习检查**：能判断 b 的形状是否能和 WX 相加，并说明扩展发生在哪个维度。

**常见误区**：依赖广播但不检查 shape，产生看似能跑、语义错误的计算。

## 7. 浅层神经网络

**来源位置**：课程1 第3周 浅层神经网络。

**重构讲解**：浅层神经网络在输入层和输出层之间加入一个隐藏层，通过非线性激活学习比 Logistic Regression 更复杂的决策边界。

**来源要点摘录**：
- # ***浅层神经网络*** # 1. 神经网络概述 ① 前面的课程中，我们已经使用计算图的方式介绍了逻辑回归梯度下降算法的正向传播和反向传播两个过程。
- # 2. 浅层神经网络 ① 下面我们以图示的方式来介绍单隐藏层的神经网络结构。

**代码或实验落点**：
- 本节重点是概念、公式或结构理解；可在章节练习中补充最小实验。

**学习检查**：能画出输入层、隐藏层、输出层，并说明 W1,b1,W2,b2 的作用。

**常见误区**：增加隐藏单元但不使用非线性激活，网络仍近似线性模型。

## 8. 激活函数

**来源位置**：神经网络表示；标题层级包括 神经网络表示、计算神经网络的输出、激活函数、使用非线性激活函数的原因、激活函数的导数、神经网络的梯度下降法。

**重构讲解**：激活函数为神经网络引入非线性，常见 sigmoid、tanh、ReLU 在输出范围、梯度特性和适用位置上不同。

**来源要点摘录**：
- ## 激活函数 有一个问题是神经网络的隐藏层和输出单元用什么激活函数。
- 之前我们都是选用 sigmoid 函数，但有时其他函数的效果会好得多。
- 然而，tanh 函数存在和 sigmoid 函数一样的缺点：当 z 趋紧无穷大（或无穷小），导数的梯度（即函数的斜率）就趋紧于 0，这使得梯度算法的速度大大减缓。
- * **ReLU 函数（the rectified linear unit，修正线性单元）**：$$a=max(0,z)$$ 当 z >

**代码或实验落点**：
- 本节重点是概念、公式或结构理解；可在章节练习中补充最小实验。

**学习检查**：能说明 ReLU 在隐藏层常用，而 sigmoid 多用于二分类输出层。

**常见误区**：所有层都机械使用 sigmoid，导致深层网络梯度变小。

## 教材式例子一：从来源结构到学习流程

以「Logistic 回归」为例，先读取来源标题层级，判断它属于概念解释、模型结构、优化策略还是实验任务；再将它映射到本章的 Logistic Regression, 损失函数, 代价函数, 计算图；最后生成一条可执行学习任务：阅读主讲义、完成练习、运行或观察实验、写出错因复盘。

## 教材式例子二：从 notebook 到课程实验

AccumulateMore/CV 中的 notebook 不是直接塞给学生，而是被拆成实验目标、关键代码用途、运行步骤和调参任务。学生学习 第 3 章 神经网络基础与向量化计算 时，应能把 `Dataset/DataLoader`、`nn.Module`、`loss.backward()`、`optimizer.step()` 或对应模型结构放回完整训练循环。

## 易错点与纠偏

- 只背来源标题，不知道标题下真正讨论了什么模型、公式或实验。
- 把 notebook 当成可直接展示的学习资源，而没有整理成目标、步骤、任务和评价。
- 学模型结构时不检查输入输出 shape，导致代码能抄但不能解释。
- 学优化或正则化时不看训练/验证曲线，无法判断方法是否真的改善泛化。

## 课堂讨论问题

1. Logistic Regression 与 损失函数 在本章流程中分别解决什么问题？
2. 如果学生已经会使用工具库，但解释不清 Logistic Regression，应该如何安排补弱？
3. 哪些内容适合做动画或流程图，哪些内容更适合做代码实验？

## 自测题

1. 围绕「Logistic Regression」写出定义、输入输出、常见误区和一个应用场景。
2. 围绕「损失函数」写出定义、输入输出、常见误区和一个应用场景。
3. 围绕「代价函数」写出定义、输入输出、常见误区和一个应用场景。
4. 围绕「计算图」写出定义、输入输出、常见误区和一个应用场景。
5. 围绕「向量化」写出定义、输入输出、常见误区和一个应用场景。
6. 围绕「广播机制」写出定义、输入输出、常见误区和一个应用场景。
7. 围绕「浅层神经网络」写出定义、输入输出、常见误区和一个应用场景。
8. 围绕「激活函数」写出定义、输入输出、常见误区和一个应用场景。
9. 围绕「Logistic Regression」写出定义、输入输出、常见误区和一个应用场景。
10. 围绕「损失函数」写出定义、输入输出、常见误区和一个应用场景。
11. 围绕「代价函数」写出定义、输入输出、常见误区和一个应用场景。
12. 围绕「计算图」写出定义、输入输出、常见误区和一个应用场景。

## 小结与下一步

完成本章后，应能把 Logistic Regression, 损失函数, 代价函数, 计算图 串成一条可执行的学习路径。下一步不是盲目堆资料，而是根据画像、诊断结果和当前章节位置选择主讲义、练习、实验或项目任务。
