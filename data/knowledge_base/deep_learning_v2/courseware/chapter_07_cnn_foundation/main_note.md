# 第 7 章 CNN 基础：卷积、池化与图像张量

## 章节定位

理解图像张量、卷积核、步幅、填充、输出尺寸、通道、局部连接、参数共享和池化。 本章不再使用概念占位模板，而是将 卷积层里的填充和步幅；计算机视觉；卷积层里的多输入多输出通道；人脸识别；目标定位；经典卷积网络；卷积原理；卷积层 中的课程结构、公式线索和 notebook 实验顺序整理为统一讲义。学生端看到的是重构后的课程内容；原始来源只作为内部依据和可追溯证据。

## 来源融合说明

- **卷积层里的填充和步幅**（AccumulateMore/CV Notebook）：覆盖 未提取标题层级。
  - 代码线索：CNN convolution example
  - 内容要点：# 1. 填充 ① 奇数卷积核更容易做padding。
  - 内容要点：我们假设卷积核大小为k * k，为了让卷积后的图像大小与原图一样大，根据公式可得到padding=（k-1）/2，这里的k只有在取奇数的时候，padding才能是整数，否则padding不好进行图片填充。
- **计算机视觉**（Andrew Ng 笔记）：覆盖 计算机视觉、卷积运算、边缘检测、更多边缘检测的例子、填充。
  - 公式线索：n \times n；f \times f；(n-f+1) \times (n-f+1)
  - 内容要点：卷积神经网络 ## 计算机视觉 **计算机视觉（Computer Vision）**的高速发展标志着新型应用产生的可能，例如自动驾驶、人脸识别、创造新的艺术风格。
  - 内容要点：人们对于计算机视觉的研究也催生了很多机算机视觉与其他领域的交叉成果。
- **卷积层里的多输入多输出通道**（AccumulateMore/CV Notebook）：覆盖 未提取标题层级。
  - 代码线索：course code example；course code example；course code example
  - 内容要点：# 1. 多个输入通道 ① 核的通道数与输入的通道数一样。
  - 内容要点：# 2. 多个输出通道 ① 每个输出通道可以匹配图片里面特定的模式。
- **人脸识别**（Andrew Ng 笔记）：覆盖 人脸识别、One-Shot 学习、Siamese 网络、Triplet 损失、二分类结构。
  - 公式线索：Similarity  = d(img1, img2)；τ；x^{(1)}
  - 内容要点：特殊应用：人脸识别和神经风格转换 ## 人脸识别 **人脸验证（Face Verification）**和**人脸识别（Face Recognition）**的区别： * 人脸验证：一般指一个一对一问题，只需要验证输入的人脸图像是否与某个已知的身份信息对应；
  - 内容要点：* 人脸识别：一个更为复杂的一对多问题，需要验证输入的人脸图像是否与多个已知身份信息中的某一个匹配。
- **目标定位**（Andrew Ng 笔记）：覆盖 目标定位、特征点检测、目标检测、基于卷积的滑动窗口实现、边框预测。
  - 公式线索：b\_x；b\_y；b\_h
  - 内容要点：目标检测 目标检测是计算机视觉领域中一个新兴的应用方向，其任务是对输入图像进行分类的同时，检测图像中是否包含某些目标，并对他们准确定位并标识。
  - 内容要点：## 目标定位 定位分类问题不仅要求判断出图片中物体的种类，还要在图片中标记出它的具体位置，用**边框（Bounding Box，或者称包围盒）**把物体圈起来。
- **经典卷积网络**（Andrew Ng 笔记）：覆盖 经典卷积网络、LeNet-5、AlexNet、VGG、残差网络。
  - 公式线索：a^{[l]}；a^{[l+2]}；z^{[l+1]} = W^{[l+1]}a^{[l]} + b^{[l+1]}
  - 内容要点：深度卷积网络：实例探究 讲到的经典 CNN 模型包括： * LeNet-5 * AlexNet * VGG 此外还有 ResNet（Residual Network，残差网络），以及 Inception Neural Network。
  - 内容要点：## 经典卷积网络 ### LeNet-5 特点： * LeNet-5 针对灰度图像而训练，因此输入图片的通道数为 1。
- **卷积原理**（AccumulateMore/CV Notebook）：覆盖 未提取标题层级。
  - 代码线索：CNN convolution example；CNN convolution example；CNN convolution example
  - 内容要点：# 1. 卷积原理 ① 卷积核不停的在原图上进行滑动，对应元素相乘再相加。
  - 内容要点：② 下图为每次滑动移动1格，然后再利用原图与卷积核上的数值进行计算得到缩略图矩阵的数据，如下图右所示。
- **卷积层**（AccumulateMore/CV Notebook）：覆盖 未提取标题层级。
  - 代码线索：Dataset/DataLoader example；Dataset/DataLoader example；Dataset/DataLoader example
  - 内容要点：# 1. 卷积原理 ① Conv1d代表一维卷积，Conv2d代表二维卷积，Conv3d代表三维卷积。
  - 内容要点：② kernel_size在训练过程中不断调整，定义为3就是3 * 3的卷积核，实际我们在训练神经网络过程中其实就是对kernel_size不断调整。

## 学习目标

- 能解释「图像张量」的定义、来源中的具体语境、公式或代码位置，并完成至少一个可检查任务。
- 能解释「卷积核」的定义、来源中的具体语境、公式或代码位置，并完成至少一个可检查任务。
- 能解释「步幅」的定义、来源中的具体语境、公式或代码位置，并完成至少一个可检查任务。
- 能解释「填充」的定义、来源中的具体语境、公式或代码位置，并完成至少一个可检查任务。
- 能解释「输出尺寸」的定义、来源中的具体语境、公式或代码位置，并完成至少一个可检查任务。
- 能解释「通道」的定义、来源中的具体语境、公式或代码位置，并完成至少一个可检查任务。
- 能解释「参数共享」的定义、来源中的具体语境、公式或代码位置，并完成至少一个可检查任务。
- 能解释「池化」的定义、来源中的具体语境、公式或代码位置，并完成至少一个可检查任务。

## 前置知识

学习本章前，应先确认上一章的核心对象、变量含义和实验边界。遇到公式时，不要只背符号，要能指出它在代码中对应哪一个张量、参数、损失或指标；遇到模型结构时，要能说清数据从输入到输出经过了哪些层。

## 知识结构总览

- **图像张量**：对应来源中的「课程综合材料」，学习时同时检查定义、流程、公式/代码和实验现象。
- **卷积核**：对应来源中的「卷积层」，学习时同时检查定义、流程、公式/代码和实验现象。
- **步幅**：对应来源中的「卷积层里的填充和步幅」，学习时同时检查定义、流程、公式/代码和实验现象。
- **填充**：对应来源中的「卷积层里的填充和步幅」，学习时同时检查定义、流程、公式/代码和实验现象。
- **输出尺寸**：对应来源中的「课程综合材料」，学习时同时检查定义、流程、公式/代码和实验现象。
- **通道**：对应来源中的「卷积层里的多输入多输出通道」，学习时同时检查定义、流程、公式/代码和实验现象。
- **参数共享**：对应来源中的「课程综合材料」，学习时同时检查定义、流程、公式/代码和实验现象。
- **池化**：对应来源中的「最大池化层」，学习时同时检查定义、流程、公式/代码和实验现象。
- **Conv2d**：对应来源中的「计算机视觉」，学习时同时检查定义、流程、公式/代码和实验现象。

## 1. 图像张量

**来源位置**：课程综合材料。

**重构讲解**：图像张量把图片表示为通道、高度、宽度和 batch 维度，CNN 的卷积、池化和归一化都依赖这个结构。

**来源要点摘录**：
- 本节作为课程组织知识点，由课程地图、章节讲义和后续实验共同支撑。

**代码或实验落点**：
- 本节重点是概念、公式或结构理解；可在章节练习中补充最小实验。

**学习检查**：能区分 HWC 和 NCHW 两种常见排列，并说明 PyTorch Conv2d 期望 NCHW。

**常见误区**：通道维度放错，导致模型输入 shape 错误或学习异常。

## 2. 卷积核

**来源位置**：卷积层。

**重构讲解**：卷积核是在局部窗口上滑动的可学习权重，用来检测边缘、纹理或更高层特征。

**来源要点摘录**：
- ② kernel_size在训练过程中不断调整，定义为3就是3 * 3的卷积核，实际我们在训练神经网络过程中其实就是对kernel_size不断调整。

**代码或实验落点**：
- Dataset/DataLoader example
- Dataset/DataLoader example
- Dataset/DataLoader example

**学习检查**：能说明 3×3 卷积核如何在输入图像上滑动并产生特征图。

**常见误区**：把卷积核当固定滤波器，不知道 CNN 中卷积核参数会被训练更新。

## 3. 步幅

**来源位置**：卷积层里的填充和步幅。

**重构讲解**：步幅决定卷积核每次移动的距离，步幅越大输出空间尺寸通常越小，计算量也会下降。

**来源要点摘录**：
- # 2. 步幅 # 3. 总结 # 1. 填充和步幅（使用框架）

**代码或实验落点**：
- CNN convolution example

**学习检查**：能用输出尺寸公式计算 stride=1 和 stride=2 的差异。

**常见误区**：只改 stride，不检查输出尺寸是否还能接入后续层。

## 4. 填充

**来源位置**：卷积层里的填充和步幅。

**重构讲解**：填充在输入边缘补零或其他值，用于控制输出尺寸并保留边缘信息。

**来源要点摘录**：
- # 1. 填充 ① 奇数卷积核更容易做padding。
- 我们假设卷积核大小为k * k，为了让卷积后的图像大小与原图一样大，根据公式可得到padding=（k-1）/2，这里的k只有在取奇数的时候，padding才能是整数，否则padding不好进行图片填充。
- ② k为偶数时，p为浮点数，所做的操作为一个为向上取整，填充，一个为向下取整，填充。
- # 2. 步幅 # 3. 总结 # 1. 填充和步幅（使用框架）

**代码或实验落点**：
- CNN convolution example

**学习检查**：能解释 same padding 为什么可让输出高宽接近输入高宽。

**常见误区**：忽略 padding 导致特征图逐层缩小过快。

## 5. 输出尺寸

**来源位置**：课程综合材料。

**重构讲解**：CNN 输出尺寸由输入尺寸、卷积核大小、填充和步幅共同决定，是调试 Conv2d 网络的第一检查项。

**来源要点摘录**：
- 本节作为课程组织知识点，由课程地图、章节讲义和后续实验共同支撑。

**代码或实验落点**：
- 本节重点是概念、公式或结构理解；可在章节练习中补充最小实验。

**学习检查**：能使用 floor((H+2P-K)/S)+1 计算卷积后的高宽。

**常见误区**：只看通道数，不计算空间尺寸，最后全连接层维度对不上。

## 6. 通道

**来源位置**：卷积层里的多输入多输出通道。

**重构讲解**：通道表示同一空间位置上的多组特征，RGB 图像有 3 个输入通道，卷积层可输出多个特征通道。

**来源要点摘录**：
- # 1. 多个输入通道 ① 核的通道数与输入的通道数一样。
- # 2. 多个输出通道 ① 每个输出通道可以匹配图片里面特定的模式。
- ② 把每个通道里面识别出来的模式组合起来，就得到组合模式识别。

**代码或实验落点**：
- course code example
- course code example
- course code example

**学习检查**：能解释 Conv2d 中 in_channels 和 out_channels 的含义。

**常见误区**：把通道数和 batch size 混淆。

## 7. 参数共享

**来源位置**：课程综合材料。

**重构讲解**：参数共享指同一个卷积核在整张图像上复用，显著减少参数量并让模型能够识别平移位置上的同类模式。

**来源要点摘录**：
- 本节作为课程组织知识点，由课程地图、章节讲义和后续实验共同支撑。

**代码或实验落点**：
- 本节重点是概念、公式或结构理解；可在章节练习中补充最小实验。

**学习检查**：能比较全连接处理图像和卷积处理图像的参数量差异。

**常见误区**：只说卷积参数少，不说明共享权重如何带来局部模式检测。

## 8. 池化

**来源位置**：最大池化层。

**重构讲解**：池化通过局部聚合降低空间尺寸，最大池化保留最强响应，平均池化保留局部平均信息。

**来源要点摘录**：
- # 1. 池化层原理 ① 最大池化层有时也被称为下采样。
- ④ 池化使得数据由5 * 5 变为3 * 3,甚至1 * 1的，这样导致计算的参数会大大减小。
- 例如1080P的电影经过池化的转为720P的电影、或360P的电影后，同样的网速下，视频更为不卡。
- # 2. 池化层处理数据 # 3. 池化层处理图片 ① 在 Anaconda 终端里面，激活py3.6.3环境，再输入 tensorboard --logdir=C:\Users\wangy\Desktop\03CV\logs 命令，将网址赋值浏览器的网址栏，回车，即可查看tensorboard显示日志情况。

**代码或实验落点**：
- PyTorch model definition
- Dataset/DataLoader example

**学习检查**：能说明 2×2 max pooling 对特征图尺寸和局部响应的影响。

**常见误区**：认为池化一定必须使用，而不考虑现代网络中 stride convolution 等替代方案。

## 9. Conv2d

**来源位置**：计算机视觉；标题层级包括 计算机视觉、卷积运算、边缘检测、更多边缘检测的例子、填充、卷积步长。

**重构讲解**：Conv2d 是 PyTorch 二维卷积层，核心参数包括 in_channels、out_channels、kernel_size、stride 和 padding。

**来源要点摘录**：
- 卷积神经网络 ## 计算机视觉 **计算机视觉（Computer Vision）**的高速发展标志着新型应用产生的可能，例如自动驾驶、人脸识别、创造新的艺术风格。
- 解决这一问题的方法就是使用**卷积神经网络（Convolutional Neural Network, CNN）**。
- ## 卷积运算 我们之前提到过，神经网络由浅层到深层，分别可以检测出图片的边缘特征、局部特征（例如眼睛、鼻子等），到最后面的一层就可以根据前面检测的特征来识别整体面部轮廓。
- **卷积运算（Convolutional Operation）**是卷积神经网络最基本的组成部分。

**代码或实验落点**：
- 本节重点是概念、公式或结构理解；可在章节练习中补充最小实验。

**学习检查**：能根据输入 shape 和 Conv2d 参数预测输出 shape。

**常见误区**：kernel_size 和 out_channels 会设置，但不理解它们分别控制局部窗口和输出特征数量。

## 教材式例子一：从来源结构到学习流程

以「卷积层里的填充和步幅」为例，先读取来源标题层级，判断它属于概念解释、模型结构、优化策略还是实验任务；再将它映射到本章的 图像张量, 卷积核, 步幅, 填充；最后生成一条可执行学习任务：阅读主讲义、完成练习、运行或观察实验、写出错因复盘。

## 教材式例子二：从 notebook 到课程实验

AccumulateMore/CV 中的 notebook 不是直接塞给学生，而是被拆成实验目标、关键代码用途、运行步骤和调参任务。学生学习 第 7 章 CNN 基础：卷积、池化与图像张量 时，应能把 `Dataset/DataLoader`、`nn.Module`、`loss.backward()`、`optimizer.step()` 或对应模型结构放回完整训练循环。

## 易错点与纠偏

- 只背来源标题，不知道标题下真正讨论了什么模型、公式或实验。
- 把 notebook 当成可直接展示的学习资源，而没有整理成目标、步骤、任务和评价。
- 学模型结构时不检查输入输出 shape，导致代码能抄但不能解释。
- 学优化或正则化时不看训练/验证曲线，无法判断方法是否真的改善泛化。

## 课堂讨论问题

1. 图像张量 与 卷积核 在本章流程中分别解决什么问题？
2. 如果学生已经会使用工具库，但解释不清 图像张量，应该如何安排补弱？
3. 哪些内容适合做动画或流程图，哪些内容更适合做代码实验？

## 自测题

1. 围绕「图像张量」写出定义、输入输出、常见误区和一个应用场景。
2. 围绕「卷积核」写出定义、输入输出、常见误区和一个应用场景。
3. 围绕「步幅」写出定义、输入输出、常见误区和一个应用场景。
4. 围绕「填充」写出定义、输入输出、常见误区和一个应用场景。
5. 围绕「输出尺寸」写出定义、输入输出、常见误区和一个应用场景。
6. 围绕「通道」写出定义、输入输出、常见误区和一个应用场景。
7. 围绕「参数共享」写出定义、输入输出、常见误区和一个应用场景。
8. 围绕「池化」写出定义、输入输出、常见误区和一个应用场景。
9. 围绕「Conv2d」写出定义、输入输出、常见误区和一个应用场景。
10. 围绕「图像张量」写出定义、输入输出、常见误区和一个应用场景。
11. 围绕「卷积核」写出定义、输入输出、常见误区和一个应用场景。
12. 围绕「步幅」写出定义、输入输出、常见误区和一个应用场景。

## 小结与下一步

完成本章后，应能把 图像张量, 卷积核, 步幅, 填充 串成一条可执行的学习路径。下一步不是盲目堆资料，而是根据画像、诊断结果和当前章节位置选择主讲义、练习、实验或项目任务。
