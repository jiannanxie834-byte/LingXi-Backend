# 第 5 章 正则化、初始化与泛化

## 章节定位

围绕偏差方差、初始化、L2、Dropout、BatchNorm、数据增强、早停与模型选择建立泛化能力。 本章不再使用概念占位模板，而是将 数据划分：训练 / 验证 / 测试集；课程2 第3周 超参数调试、Batch正则化和编程框架；课程2 第1周 作业题 深度神经网络的初始化 正则化 梯度检验；batch 梯度下降法；超参数调试处理；权重衰退；数值稳定性、模型初始化、激活函数；Logistic 回归 中的课程结构、公式线索和 notebook 实验顺序整理为统一讲义。学生端看到的是重构后的课程内容；原始来源只作为内部依据和可追溯证据。

## 来源融合说明

- **数据划分：训练 / 验证 / 测试集**（Andrew Ng 笔记）：覆盖 数据划分：训练 / 验证 / 测试集、建议、补充：交叉验证（cross validation）、参考资料、模型估计：偏差 / 方差。
  - 公式线索：J(w,b) = \frac{1}{m}\sum_{i=1}^mL(\hat{y}^{(i)},y^{(i)})+\frac{\lambda}{2m}{||w||}^2\_2；\frac{\lambda}{2m}{||w||}^2\_2 = \frac{\lambda}{2m}\sum_{j=1}^{n\_x}w^2\_j = \frac{\lambda}{2m}w^Tw；\frac{\lambda}{2m}{||w||}\_1 = \frac{\lambda}{2m}\sum_{j=1}^{n\_x}{|w\_j|}
  - 内容要点：深度学习的实用层面 ## 数据划分：训练 / 验证 / 测试集 应用深度学习是一个典型的迭代过程。
  - 内容要点：对于一个需要解决的问题的样本数据，在建立模型的过程中，数据会被划分为以下几个部分： * 训练集（train set）：用训练集对算法或模型进行**训练**过程；
- **课程2 第3周 超参数调试、Batch正则化和编程框架**（AccumulateMore/CV Notebook）：覆盖 未提取标题层级。
  - 代码线索：course code example；training loop and optimizer；course code example
  - 内容要点：# ***超参数调试、Batch正则化和编程框架*** # 1. 超参数 ① 深度神经网络需要调试的超参数（Hyperparameters）较多，包括： ① 超参数之间也有重要性差异。
  - 内容要点：② 通常来说，学习因子α是最重要的超参数，也是需要重点调试的超参数。
- **课程2 第1周 作业题 深度神经网络的初始化 正则化 梯度检验**（AccumulateMore/CV Notebook）：覆盖 未提取标题层级。
  - 代码线索：course code example；course code example；course code example
  - 内容要点：# ***笔记前言*** 1. 初始化参数： - 1.1：使用0来初始化参数。
  - 内容要点：- 1.3：使用抑梯度异常初始化参数（参见视频中的梯度消失和梯度爆炸）。
- **batch 梯度下降法**（Andrew Ng 笔记）：覆盖 batch 梯度下降法、Mini-Batch 梯度下降法、batch 的不同大小（size）带来的影响、mini-batch 大小的选择、获得 mini-batch 的步骤。
  - 公式线索：2^6；2^7；2^9
  - 内容要点：优化算法 深度学习难以在大数据领域发挥最大效果的一个原因是，在巨大的数据集基础上进行训练速度很慢。
  - 内容要点：## batch 梯度下降法 **batch 梯度下降法**（批梯度下降法，我们之前一直使用的梯度下降法）是最常用的梯度下降形式，即同时处理整个训练集。
- **超参数调试处理**（Andrew Ng 笔记）：覆盖 超参数调试处理、重要程度排序、调参技巧、选择合适的范围、一些建议。
  - 公式线索：10^{-8}；a^{[l]}；W^{[l+1]}
  - 内容要点：超参数调试、Batch 正则化和程序框架 ## 超参数调试处理 ### 重要程度排序 目前已经讲到过的超参数中，重要程度依次是（仅供参考）： * **最重要**： * 学习率 α；
  - 内容要点：* **其次重要**： * β：动量衰减参数，常设置为 0.9；
- **权重衰退**（AccumulateMore/CV Notebook）：覆盖 未提取标题层级。
  - 代码线索：course code example；course code example；course code example
  - 内容要点：# 1. 权重衰退 ① 权重衰退是常见的处理过拟合的一种方法。
  - 内容要点：② 把模型容量控制比较小有两种方法，方法一：模型控制的比较小，使得模型中参数比较少。
- **数值稳定性、模型初始化、激活函数**（AccumulateMore/CV Notebook）：覆盖 未提取标题层级。
  - 内容要点：② 对角矩阵(diagonal matrix)是一个主对角线之外的元素皆为0的矩阵，常写为diag(a1, a2, ..., an)。
  - 内容要点：这就是个链式求导，diag是n维度的relu向量對n维度relu的输入的求导，向量对自身求导就是对角矩阵。
- **Logistic 回归**（Andrew Ng 笔记）：覆盖 Logistic 回归、损失函数、梯度下降法（Gradient Descent）、计算图（Computation Graph）、Logistic 回归中的梯度下降法。
  - 公式线索：x \in R^{n\_x}；{n\_x}；y \in 0,1
  - 内容要点：神经网络基础 * 实现一个神经网络时，如果需要遍历整个训练集，并不需要直接使用 for 循环。
  - 内容要点：* 神经网络的计算过程中，通常有一个正向过程（forward pass）或者叫**正向传播步骤（forward propagation step）**，接着会有一个反向过程（backward pass）或者叫**反向传播步骤（backward propagation step）**。

## 学习目标

- 能解释「训练/验证/测试划分」的定义、来源中的具体语境、公式或代码位置，并完成至少一个可检查任务。
- 能解释「偏差方差」的定义、来源中的具体语境、公式或代码位置，并完成至少一个可检查任务。
- 能解释「初始化」的定义、来源中的具体语境、公式或代码位置，并完成至少一个可检查任务。
- 能解释「L2 正则化」的定义、来源中的具体语境、公式或代码位置，并完成至少一个可检查任务。
- 能解释「Dropout」的定义、来源中的具体语境、公式或代码位置，并完成至少一个可检查任务。
- 能解释「BatchNorm」的定义、来源中的具体语境、公式或代码位置，并完成至少一个可检查任务。
- 能解释「数据增强」的定义、来源中的具体语境、公式或代码位置，并完成至少一个可检查任务。
- 能解释「早停」的定义、来源中的具体语境、公式或代码位置，并完成至少一个可检查任务。

## 前置知识

学习本章前，应先确认上一章的核心对象、变量含义和实验边界。遇到公式时，不要只背符号，要能指出它在代码中对应哪一个张量、参数、损失或指标；遇到模型结构时，要能说清数据从输入到输出经过了哪些层。

## 知识结构总览

- **训练/验证/测试划分**：对应来源中的「数据划分：训练 / 验证 / 测试集」，学习时同时检查定义、流程、公式/代码和实验现象。
- **偏差方差**：对应来源中的「数据划分：训练 / 验证 / 测试集」，学习时同时检查定义、流程、公式/代码和实验现象。
- **初始化**：对应来源中的「课程2 第1周 作业题 深度神经网络的初始化 正则化 梯度检验」，学习时同时检查定义、流程、公式/代码和实验现象。
- **L2 正则化**：对应来源中的「数据划分：训练 / 验证 / 测试集」，学习时同时检查定义、流程、公式/代码和实验现象。
- **Dropout**：对应来源中的「课程综合材料」，学习时同时检查定义、流程、公式/代码和实验现象。
- **BatchNorm**：对应来源中的「超参数调试处理」，学习时同时检查定义、流程、公式/代码和实验现象。
- **数据增强**：对应来源中的「课程综合材料」，学习时同时检查定义、流程、公式/代码和实验现象。
- **早停**：对应来源中的「课程综合材料」，学习时同时检查定义、流程、公式/代码和实验现象。

## 1. 训练/验证/测试划分

**来源位置**：数据划分：训练 / 验证 / 测试集；标题层级包括 数据划分：训练 / 验证 / 测试集、建议、补充：交叉验证（cross validation）、参考资料、模型估计：偏差 / 方差、应对方法。

**重构讲解**：训练集用于拟合参数，验证集用于调模型和超参数，测试集用于最终估计泛化性能，三者职责不能混用。

**来源要点摘录**：
- 深度学习的实用层面 ## 数据划分：训练 / 验证 / 测试集 应用深度学习是一个典型的迭代过程。
- 对于一个需要解决的问题的样本数据，在建立模型的过程中，数据会被划分为以下几个部分： * 训练集（train set）：用训练集对算法或模型进行**训练**过程；
- * 验证集（development set）：利用验证集（又称为简单交叉验证集，hold-out cross validation set）进行**交叉验证**，**选择出最好的模型**；
- * 测试集（test set）：最后利用测试集对模型进行测试，**获取模型运行的无偏估计**（对学习方法进行评估）。

**代码或实验落点**：
- 本节重点是概念、公式或结构理解；可在章节练习中补充最小实验。

**学习检查**：能说明为什么不能根据测试集结果反复调参。

**常见误区**：把验证集当测试集反复使用，导致泛化评估过于乐观。

## 2. 偏差方差

**来源位置**：数据划分：训练 / 验证 / 测试集；标题层级包括 数据划分：训练 / 验证 / 测试集、建议、补充：交叉验证（cross validation）、参考资料、模型估计：偏差 / 方差、应对方法。

**重构讲解**：偏差反映模型拟合能力不足，方差反映模型对训练数据过于敏感；二者帮助判断欠拟合和过拟合。

**来源要点摘录**：
- ### 参考资料 无偏估计_百度百科 ## 模型估计：偏差 / 方差 **“偏差-方差分解”（bias-variance decomposition）**是解释学习算法泛化性能的一种重要工具。
- 泛化误差可分解为偏差、方差与噪声之和： * **偏差**：度量了学习算法的期望预测与真实结果的偏离程度，即刻画了**学习算法本身的拟合能力**；
- * **方差**：度量了同样大小的训练集的变动所导致的学习性能的变化，即刻画了**数据扰动所造成的影响**；
- 偏差-方差分解说明，**泛化性能**是由**学习算法的能力**、**数据的充分性**以及**学习任务本身的难度**所共同决定的。

**代码或实验落点**：
- 本节重点是概念、公式或结构理解；可在章节练习中补充最小实验。

**学习检查**：能根据 train/dev error 判断高偏差、高方差或两者兼有。

**常见误区**：看到验证误差高就盲目加深模型，不先区分偏差和方差。

## 3. 初始化

**来源位置**：课程2 第1周 作业题 深度神经网络的初始化 正则化 梯度检验。

**重构讲解**：初始化决定训练开始时参数尺度和对称性，合适初始化能保持激活和梯度在合理范围内，减少训练不稳定。

**来源要点摘录**：
- # ***笔记前言*** 1. 初始化参数： - 1.1：使用0来初始化参数。
- - 1.3：使用抑梯度异常初始化参数（参见视频中的梯度消失和梯度爆炸）。
- # ***深度神经网络的初始化*** ① 训练神经网络需要指定权重的初始值，而一个好的初始化方法将有助于网络学习。
- ② 如果你完成了本系列的上一课程，则可能已经按照我们的说明完成了权重初始化。

**代码或实验落点**：
- course code example
- course code example
- course code example

**学习检查**：能说明为什么全零初始化会让同层神经元学习相同特征。

**常见误区**：随意初始化权重，不观察梯度和激活是否饱和。

## 4. L2 正则化

**来源位置**：数据划分：训练 / 验证 / 测试集；标题层级包括 数据划分：训练 / 验证 / 测试集、建议、补充：交叉验证（cross validation）、参考资料、模型估计：偏差 / 方差、应对方法。

**公式/结构线索**：`J_reg = J + lambda/(2m) ||W||_2^2`。

**重构讲解**：L2 正则化在代价函数中惩罚较大的权重，促使模型参数更平滑，降低过拟合风险。

**来源要点摘录**：
- 本节作为课程组织知识点，由课程地图、章节讲义和后续实验共同支撑。

**代码或实验落点**：
- 本节重点是概念、公式或结构理解；可在章节练习中补充最小实验。

**学习检查**：能说明正则化强度 lambda 增大时训练误差和验证误差可能如何变化。

**常见误区**：把 L2 当作一定提升准确率的技巧，不结合过拟合程度选择。

## 5. Dropout

**来源位置**：课程综合材料。

**公式/结构线索**：`A_drop = A * mask / keep_prob`。

**重构讲解**：Dropout 在训练时随机关闭部分神经元，迫使网络不能过度依赖某些特征组合，起到正则化作用。

**来源要点摘录**：
- 本节作为课程组织知识点，由课程地图、章节讲义和后续实验共同支撑。

**代码或实验落点**：
- 本节重点是概念、公式或结构理解；可在章节练习中补充最小实验。

**学习检查**：能说明训练阶段和推理阶段 Dropout 行为不同。

**常见误区**：在验证或测试阶段仍然启用 Dropout，导致结果随机波动。

## 6. BatchNorm

**来源位置**：超参数调试处理；标题层级包括 超参数调试处理、重要程度排序、调参技巧、选择合适的范围、一些建议、Batch Normalization。

**公式/结构线索**：`z_hat = (z - mu) / sqrt(sigma^2 + eps)`。

**重构讲解**：BatchNorm 在小批量内标准化中间激活，并学习缩放和平移参数，可以稳定训练、允许更大学习率，并在 CNN 中常和卷积层配合使用。

**来源要点摘录**：
- 超参数调试、Batch 正则化和程序框架 ## 超参数调试处理 ### 重要程度排序 目前已经讲到过的超参数中，重要程度依次是（仅供参考）： * **最重要**： * 学习率 α；
- ## Batch Normalization **批标准化（Batch Normalization，经常简称为 BN）**会使参数搜索问题变得很容易，使神经网络对超参数的选择更加稳定，超参数的范围会更庞大，工作效果也很好，也会使训练更容易。

**代码或实验落点**：
- 本节重点是概念、公式或结构理解；可在章节练习中补充最小实验。

**学习检查**：能区分 BatchNorm 在训练阶段使用 batch 统计量、推理阶段使用移动平均统计量。

**常见误区**：把 BatchNorm 当成输入数据预处理，而不是网络内部层。

## 7. 数据增强

**来源位置**：课程综合材料。

**重构讲解**：数据增强通过裁剪、翻转、颜色扰动等方式扩展训练分布，尤其适合图像任务提升泛化能力。

**来源要点摘录**：
- 本节作为课程组织知识点，由课程地图、章节讲义和后续实验共同支撑。

**代码或实验落点**：
- 本节重点是概念、公式或结构理解；可在章节练习中补充最小实验。

**学习检查**：能区分训练增强和测试预处理的不同目标。

**常见误区**：使用改变语义的增强，例如把数字或医学图像随意翻转。

## 8. 早停

**来源位置**：课程综合材料。

**重构讲解**：早停根据验证集表现停止训练，避免模型在训练集上继续拟合噪声，是一种实践中的正则化策略。

**来源要点摘录**：
- 本节作为课程组织知识点，由课程地图、章节讲义和后续实验共同支撑。

**代码或实验落点**：
- 本节重点是概念、公式或结构理解；可在章节练习中补充最小实验。

**学习检查**：能根据验证 loss 连续不下降判断何时停止并保存最佳模型。

**常见误区**：只保存最后一轮模型，而不是验证集表现最好的模型。

## 教材式例子一：从来源结构到学习流程

以「数据划分：训练 / 验证 / 测试集」为例，先读取来源标题层级，判断它属于概念解释、模型结构、优化策略还是实验任务；再将它映射到本章的 训练/验证/测试划分, 偏差方差, 初始化, L2 正则化；最后生成一条可执行学习任务：阅读主讲义、完成练习、运行或观察实验、写出错因复盘。

## 教材式例子二：从 notebook 到课程实验

AccumulateMore/CV 中的 notebook 不是直接塞给学生，而是被拆成实验目标、关键代码用途、运行步骤和调参任务。学生学习 第 5 章 正则化、初始化与泛化 时，应能把 `Dataset/DataLoader`、`nn.Module`、`loss.backward()`、`optimizer.step()` 或对应模型结构放回完整训练循环。

## 易错点与纠偏

- 只背来源标题，不知道标题下真正讨论了什么模型、公式或实验。
- 把 notebook 当成可直接展示的学习资源，而没有整理成目标、步骤、任务和评价。
- 学模型结构时不检查输入输出 shape，导致代码能抄但不能解释。
- 学优化或正则化时不看训练/验证曲线，无法判断方法是否真的改善泛化。

## 课堂讨论问题

1. 训练/验证/测试划分 与 偏差方差 在本章流程中分别解决什么问题？
2. 如果学生已经会使用工具库，但解释不清 训练/验证/测试划分，应该如何安排补弱？
3. 哪些内容适合做动画或流程图，哪些内容更适合做代码实验？

## 自测题

1. 围绕「训练/验证/测试划分」写出定义、输入输出、常见误区和一个应用场景。
2. 围绕「偏差方差」写出定义、输入输出、常见误区和一个应用场景。
3. 围绕「初始化」写出定义、输入输出、常见误区和一个应用场景。
4. 围绕「L2 正则化」写出定义、输入输出、常见误区和一个应用场景。
5. 围绕「Dropout」写出定义、输入输出、常见误区和一个应用场景。
6. 围绕「BatchNorm」写出定义、输入输出、常见误区和一个应用场景。
7. 围绕「数据增强」写出定义、输入输出、常见误区和一个应用场景。
8. 围绕「早停」写出定义、输入输出、常见误区和一个应用场景。
9. 围绕「训练/验证/测试划分」写出定义、输入输出、常见误区和一个应用场景。
10. 围绕「偏差方差」写出定义、输入输出、常见误区和一个应用场景。
11. 围绕「初始化」写出定义、输入输出、常见误区和一个应用场景。
12. 围绕「L2 正则化」写出定义、输入输出、常见误区和一个应用场景。

## 小结与下一步

完成本章后，应能把 训练/验证/测试划分, 偏差方差, 初始化, L2 正则化 串成一条可执行的学习路径。下一步不是盲目堆资料，而是根据画像、诊断结果和当前章节位置选择主讲义、练习、实验或项目任务。
