# 第 5 章 正则化、初始化与泛化

## 章节定位

围绕偏差方差、初始化、L2、Dropout、BatchNorm、数据增强、早停与模型选择建立泛化能力。 本章整合参考来源：经典卷积网络；人脸识别；batch 梯度下降法；数据划分：训练 / 验证 / 测试集；超参数调试处理；神经网络表示；深层网络中的前向和反向传播；Logistic 回归；数学符号；正交化。课程讲义采用重新组织后的教材式表达，用于学生学习、RAG 检索和个性化资源生成。

## 学习目标

- 能解释「训练/验证/测试划分」在本章中的作用、输入输出和常见误区。
- 能解释「偏差方差」在本章中的作用、输入输出和常见误区。
- 能解释「初始化」在本章中的作用、输入输出和常见误区。
- 能解释「L2 正则化」在本章中的作用、输入输出和常见误区。
- 能解释「Dropout」在本章中的作用、输入输出和常见误区。
- 能解释「BatchNorm」在本章中的作用、输入输出和常见误区。
- 能解释「数据增强」在本章中的作用、输入输出和常见误区。
- 能解释「早停」在本章中的作用、输入输出和常见误区。

## 前置知识

学习本章前，应确认自己能够说明上一章的核心对象、变量含义和实验边界。遇到公式时，不要求先记住推导细节，但要能说清每个符号代表什么，以及它在代码或实验记录中对应哪一个张量、指标或超参数。

## 知识结构总览

- **训练/验证/测试划分**：先理解定义，再把它放入完整训练流程中观察。
- **偏差方差**：先理解定义，再把它放入完整训练流程中观察。
- **初始化**：先理解定义，再把它放入完整训练流程中观察。
- **L2 正则化**：先理解定义，再把它放入完整训练流程中观察。
- **Dropout**：先理解定义，再把它放入完整训练流程中观察。
- **BatchNorm**：先理解定义，再把它放入完整训练流程中观察。
- **数据增强**：先理解定义，再把它放入完整训练流程中观察。
- **早停**：先理解定义，再把它放入完整训练流程中观察。

## 1. 训练/验证/测试划分

训练/验证/测试划分 不是孤立术语，而是本章任务链中的一个决策点。学习时先问三个问题：它接收什么输入、产生什么输出、改变了模型训练或推理中的哪一步。在课程资源生成时，系统会把它作为知识单元记录前置关系、练习类型和可视化建议。

例子：如果把「训练/验证/测试划分」用于一个小型图像或文本任务，应明确数据形状、模型层、损失或评价指标之间的对应关系。常见错误是只背名字，不检查张量维度、训练/验证边界或实验假设。

## 2. 偏差方差

偏差方差 不是孤立术语，而是本章任务链中的一个决策点。学习时先问三个问题：它接收什么输入、产生什么输出、改变了模型训练或推理中的哪一步。在课程资源生成时，系统会把它作为知识单元记录前置关系、练习类型和可视化建议。

例子：如果把「偏差方差」用于一个小型图像或文本任务，应明确数据形状、模型层、损失或评价指标之间的对应关系。常见错误是只背名字，不检查张量维度、训练/验证边界或实验假设。

## 3. 初始化

初始化 不是孤立术语，而是本章任务链中的一个决策点。学习时先问三个问题：它接收什么输入、产生什么输出、改变了模型训练或推理中的哪一步。在课程资源生成时，系统会把它作为知识单元记录前置关系、练习类型和可视化建议。

例子：如果把「初始化」用于一个小型图像或文本任务，应明确数据形状、模型层、损失或评价指标之间的对应关系。常见错误是只背名字，不检查张量维度、训练/验证边界或实验假设。

## 4. L2 正则化

L2 正则化 不是孤立术语，而是本章任务链中的一个决策点。学习时先问三个问题：它接收什么输入、产生什么输出、改变了模型训练或推理中的哪一步。在课程资源生成时，系统会把它作为知识单元记录前置关系、练习类型和可视化建议。

常用表达式：`J_reg = J + lambda/(2m) ||W||_2^2`。

例子：如果把「L2 正则化」用于一个小型图像或文本任务，应明确数据形状、模型层、损失或评价指标之间的对应关系。常见错误是只背名字，不检查张量维度、训练/验证边界或实验假设。

## 5. Dropout

Dropout 不是孤立术语，而是本章任务链中的一个决策点。学习时先问三个问题：它接收什么输入、产生什么输出、改变了模型训练或推理中的哪一步。在课程资源生成时，系统会把它作为知识单元记录前置关系、练习类型和可视化建议。

常用表达式：`A_drop = A * mask / keep_prob`。

例子：如果把「Dropout」用于一个小型图像或文本任务，应明确数据形状、模型层、损失或评价指标之间的对应关系。常见错误是只背名字，不检查张量维度、训练/验证边界或实验假设。

## 6. BatchNorm

BatchNorm 不是孤立术语，而是本章任务链中的一个决策点。学习时先问三个问题：它接收什么输入、产生什么输出、改变了模型训练或推理中的哪一步。在课程资源生成时，系统会把它作为知识单元记录前置关系、练习类型和可视化建议。

常用表达式：`z_hat = (z - mu) / sqrt(sigma^2 + eps)`。

例子：如果把「BatchNorm」用于一个小型图像或文本任务，应明确数据形状、模型层、损失或评价指标之间的对应关系。常见错误是只背名字，不检查张量维度、训练/验证边界或实验假设。

## 7. 数据增强

数据增强 不是孤立术语，而是本章任务链中的一个决策点。学习时先问三个问题：它接收什么输入、产生什么输出、改变了模型训练或推理中的哪一步。在课程资源生成时，系统会把它作为知识单元记录前置关系、练习类型和可视化建议。

例子：如果把「数据增强」用于一个小型图像或文本任务，应明确数据形状、模型层、损失或评价指标之间的对应关系。常见错误是只背名字，不检查张量维度、训练/验证边界或实验假设。

## 8. 早停

早停 不是孤立术语，而是本章任务链中的一个决策点。学习时先问三个问题：它接收什么输入、产生什么输出、改变了模型训练或推理中的哪一步。在课程资源生成时，系统会把它作为知识单元记录前置关系、练习类型和可视化建议。

例子：如果把「早停」用于一个小型图像或文本任务，应明确数据形状、模型层、损失或评价指标之间的对应关系。常见错误是只背名字，不检查张量维度、训练/验证边界或实验假设。

## 教材式例子一：从概念到流程

以「训练/验证/测试划分」为起点，先画出输入、处理模块和输出；再标注可学习参数、非参数操作和评价指标；最后说明如果结果不理想，应该检查数据、模型容量、损失函数还是优化策略。这个例子训练的是结构化表达能力，而不是让学生机械复述定义。

## 教材式例子二：从流程到代码

将本章知识落到 PyTorch 时，需要把概念映射为 `Tensor`、`nn.Module`、`loss`、`optimizer`、`train/eval` 等对象。即使本章不是纯代码章节，也要保持“概念-公式-实验”的闭环。

## 易错点与纠偏

- 只关注结论，不记录实验条件，导致无法复现或比较。
- 把相关知识当作主标题，忽略用户当前真正要学的主题。
- 在没有证据的情况下推断学生水平，造成推荐过难或过浅。
- 忽略前置知识，使学习路线看似完整但执行困难。

## 课堂讨论问题

1. 训练/验证/测试划分 与 偏差方差 在本章流程中分别解决什么问题？
2. 如果学生已经会使用工具库，但解释不清 训练/验证/测试划分，应该如何安排补弱？
3. 哪些内容适合做动画或流程图，哪些内容更适合做代码实验？

## 自测题

1. 围绕「训练/验证/测试划分」写出定义、输入输出、常见误区和一个应用场景。
2. 围绕「偏差方差」写出定义、输入输出、常见误区和一个应用场景。
3. 围绕「初始化」写出定义、输入输出、常见误区和一个应用场景。
4. 围绕「L2 正则化」写出定义、输入输出、常见误区和一个应用场景。
5. 围绕「Dropout」写出定义、输入输出、常见误区和一个应用场景。
6. 围绕「BatchNorm」写出定义、输入输出、常见误区和一个应用场景。
7. 围绕「数据增强」写出定义、输入输出、常见误区和一个应用场景。
8. 围绕「早停」写出定义、输入输出、常见误区和一个应用场景。
9. 围绕「训练/验证/测试划分」写出定义、输入输出、常见误区和一个应用场景。
10. 围绕「偏差方差」写出定义、输入输出、常见误区和一个应用场景。
11. 围绕「初始化」写出定义、输入输出、常见误区和一个应用场景。
12. 围绕「L2 正则化」写出定义、输入输出、常见误区和一个应用场景。

## 小结与下一步

完成本章后，应能把 训练/验证/测试划分, 偏差方差, 初始化, L2 正则化 串成一条可执行的学习路径。下一步不是盲目堆资料，而是根据画像、诊断结果和当前章节位置选择主讲义、练习、实验或项目任务。

## 深入学习提示

为了避免浅层学习，建议按照“概念复述、公式解释、流程图、代码定位、实验记录、错因复盘”六步完成本章。每一步都要产出可检查结果：一句定义、一张图、一个最小代码片段、一组训练曲线或一段错因说明。系统后续进行个性化推送时，会优先根据这些产出判断学生更需要讲义、题集、代码实验还是项目任务。

## 深入学习提示

为了避免浅层学习，建议按照“概念复述、公式解释、流程图、代码定位、实验记录、错因复盘”六步完成本章。每一步都要产出可检查结果：一句定义、一张图、一个最小代码片段、一组训练曲线或一段错因说明。系统后续进行个性化推送时，会优先根据这些产出判断学生更需要讲义、题集、代码实验还是项目任务。

## 深入学习提示

为了避免浅层学习，建议按照“概念复述、公式解释、流程图、代码定位、实验记录、错因复盘”六步完成本章。每一步都要产出可检查结果：一句定义、一张图、一个最小代码片段、一组训练曲线或一段错因说明。系统后续进行个性化推送时，会优先根据这些产出判断学生更需要讲义、题集、代码实验还是项目任务。

## 深入学习提示

为了避免浅层学习，建议按照“概念复述、公式解释、流程图、代码定位、实验记录、错因复盘”六步完成本章。每一步都要产出可检查结果：一句定义、一张图、一个最小代码片段、一组训练曲线或一段错因说明。系统后续进行个性化推送时，会优先根据这些产出判断学生更需要讲义、题集、代码实验还是项目任务。

## 深入学习提示

为了避免浅层学习，建议按照“概念复述、公式解释、流程图、代码定位、实验记录、错因复盘”六步完成本章。每一步都要产出可检查结果：一句定义、一张图、一个最小代码片段、一组训练曲线或一段错因说明。系统后续进行个性化推送时，会优先根据这些产出判断学生更需要讲义、题集、代码实验还是项目任务。

## 深入学习提示

为了避免浅层学习，建议按照“概念复述、公式解释、流程图、代码定位、实验记录、错因复盘”六步完成本章。每一步都要产出可检查结果：一句定义、一张图、一个最小代码片段、一组训练曲线或一段错因说明。系统后续进行个性化推送时，会优先根据这些产出判断学生更需要讲义、题集、代码实验还是项目任务。

## 深入学习提示

为了避免浅层学习，建议按照“概念复述、公式解释、流程图、代码定位、实验记录、错因复盘”六步完成本章。每一步都要产出可检查结果：一句定义、一张图、一个最小代码片段、一组训练曲线或一段错因说明。系统后续进行个性化推送时，会优先根据这些产出判断学生更需要讲义、题集、代码实验还是项目任务。

## 深入学习提示

为了避免浅层学习，建议按照“概念复述、公式解释、流程图、代码定位、实验记录、错因复盘”六步完成本章。每一步都要产出可检查结果：一句定义、一张图、一个最小代码片段、一组训练曲线或一段错因说明。系统后续进行个性化推送时，会优先根据这些产出判断学生更需要讲义、题集、代码实验还是项目任务。

## 深入学习提示

为了避免浅层学习，建议按照“概念复述、公式解释、流程图、代码定位、实验记录、错因复盘”六步完成本章。每一步都要产出可检查结果：一句定义、一张图、一个最小代码片段、一组训练曲线或一段错因说明。系统后续进行个性化推送时，会优先根据这些产出判断学生更需要讲义、题集、代码实验还是项目任务。

## 深入学习提示

为了避免浅层学习，建议按照“概念复述、公式解释、流程图、代码定位、实验记录、错因复盘”六步完成本章。每一步都要产出可检查结果：一句定义、一张图、一个最小代码片段、一组训练曲线或一段错因说明。系统后续进行个性化推送时，会优先根据这些产出判断学生更需要讲义、题集、代码实验还是项目任务。

## 深入学习提示

为了避免浅层学习，建议按照“概念复述、公式解释、流程图、代码定位、实验记录、错因复盘”六步完成本章。每一步都要产出可检查结果：一句定义、一张图、一个最小代码片段、一组训练曲线或一段错因说明。系统后续进行个性化推送时，会优先根据这些产出判断学生更需要讲义、题集、代码实验还是项目任务。

## 深入学习提示

为了避免浅层学习，建议按照“概念复述、公式解释、流程图、代码定位、实验记录、错因复盘”六步完成本章。每一步都要产出可检查结果：一句定义、一张图、一个最小代码片段、一组训练曲线或一段错因说明。系统后续进行个性化推送时，会优先根据这些产出判断学生更需要讲义、题集、代码实验还是项目任务。

## 深入学习提示

为了避免浅层学习，建议按照“概念复述、公式解释、流程图、代码定位、实验记录、错因复盘”六步完成本章。每一步都要产出可检查结果：一句定义、一张图、一个最小代码片段、一组训练曲线或一段错因说明。系统后续进行个性化推送时，会优先根据这些产出判断学生更需要讲义、题集、代码实验还是项目任务。

## 深入学习提示

为了避免浅层学习，建议按照“概念复述、公式解释、流程图、代码定位、实验记录、错因复盘”六步完成本章。每一步都要产出可检查结果：一句定义、一张图、一个最小代码片段、一组训练曲线或一段错因说明。系统后续进行个性化推送时，会优先根据这些产出判断学生更需要讲义、题集、代码实验还是项目任务。

## 深入学习提示

为了避免浅层学习，建议按照“概念复述、公式解释、流程图、代码定位、实验记录、错因复盘”六步完成本章。每一步都要产出可检查结果：一句定义、一张图、一个最小代码片段、一组训练曲线或一段错因说明。系统后续进行个性化推送时，会优先根据这些产出判断学生更需要讲义、题集、代码实验还是项目任务。

## 深入学习提示

为了避免浅层学习，建议按照“概念复述、公式解释、流程图、代码定位、实验记录、错因复盘”六步完成本章。每一步都要产出可检查结果：一句定义、一张图、一个最小代码片段、一组训练曲线或一段错因说明。系统后续进行个性化推送时，会优先根据这些产出判断学生更需要讲义、题集、代码实验还是项目任务。
