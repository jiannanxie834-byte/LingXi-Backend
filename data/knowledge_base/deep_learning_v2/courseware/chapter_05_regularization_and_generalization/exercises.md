# 第 5 章 正则化、初始化与泛化 练习题集

## 1. 选择题：训练/验证/测试划分

题目：结合来源「数据划分：训练 / 验证 / 测试集」（标题线索：数据划分：训练 / 验证 / 测试集；建议；补充：交叉验证（cross validation）；参考资料），完成关于「训练/验证/测试划分」的分析：它解决什么问题、核心机制是什么、如何在实验或代码中验证？

答案：训练集用于拟合参数，验证集用于调模型和超参数，测试集用于最终估计泛化性能，三者职责不能混用。 本题可使用公式 `J(w,b) = \frac{1}{m}\sum_{i=1}^mL(\hat{y}^{(i)},y^{(i)})+\frac{\lambda}{2m}{||w||}^2\_2` 辅助解释。

解析：本题的来源证据是：深度学习的实用层面 ## 数据划分：训练 / 验证 / 测试集 应用深度学习是一个典型的迭代过程。。能说明为什么不能根据测试集结果反复调参。 正确答案还应从「数据划分：训练 / 验证 / 测试集」中提取一个证据点，例如标题层级、公式、结构、代码目的或实验现象。

常见错误：把验证集当测试集反复使用，导致泛化评估过于乐观。

## 2. 判断题：偏差方差

题目：结合来源「数据划分：训练 / 验证 / 测试集」（标题线索：数据划分：训练 / 验证 / 测试集；建议；补充：交叉验证（cross validation）；参考资料），完成关于「偏差方差」的分析：它解决什么问题、核心机制是什么、如何在实验或代码中验证？

答案：偏差反映模型拟合能力不足，方差反映模型对训练数据过于敏感；二者帮助判断欠拟合和过拟合。 本题可使用公式 `J(w,b) = \frac{1}{m}\sum_{i=1}^mL(\hat{y}^{(i)},y^{(i)})+\frac{\lambda}{2m}{||w||}^2\_2` 辅助解释。

解析：本题的来源证据是：### 参考资料 无偏估计_百度百科 ## 模型估计：偏差 / 方差 **“偏差-方差分解”（bias-variance decomposition）**是解释学习算法泛化性能的一种重要工具。。能根据 train/dev error 判断高偏差、高方差或两者兼有。 正确答案还应从「数据划分：训练 / 验证 / 测试集」中提取一个证据点，例如标题层级、公式、结构、代码目的或实验现象。

常见错误：看到验证误差高就盲目加深模型，不先区分偏差和方差。

## 3. 简答题：初始化

题目：结合来源「课程2 第1周 作业题 深度神经网络的初始化 正则化 梯度检验」，完成关于「初始化」的分析：它解决什么问题、核心机制是什么、如何在实验或代码中验证？

答案：初始化决定训练开始时参数尺度和对称性，合适初始化能保持激活和梯度在合理范围内，减少训练不稳定。 

解析：本题的来源证据是：# ***笔记前言*** 1. 初始化参数： - 1.1：使用0来初始化参数。。能说明为什么全零初始化会让同层神经元学习相同特征。 正确答案还应从「课程2 第1周 作业题 深度神经网络的初始化 正则化 梯度检验」中提取一个证据点，例如标题层级、公式、结构、代码目的或实验现象。

常见错误：随意初始化权重，不观察梯度和激活是否饱和。

## 4. 计算/推导题：L2 正则化

题目：结合来源「数据划分：训练 / 验证 / 测试集」（标题线索：数据划分：训练 / 验证 / 测试集；建议；补充：交叉验证（cross validation）；参考资料），完成关于「L2 正则化」的分析：它解决什么问题、核心机制是什么、如何在实验或代码中验证？

答案：L2 正则化在代价函数中惩罚较大的权重，促使模型参数更平滑，降低过拟合风险。 本题可使用公式 `J_reg = J + lambda/(2m) ||W||_2^2` 辅助解释。

解析：本题的来源证据是：本题主要依据课程地图、章节讲义和实验任务综合作答。。能说明正则化强度 lambda 增大时训练误差和验证误差可能如何变化。 正确答案还应从「数据划分：训练 / 验证 / 测试集」中提取一个证据点，例如标题层级、公式、结构、代码目的或实验现象。

常见错误：把 L2 当作一定提升准确率的技巧，不结合过拟合程度选择。

## 5. 代码理解题：Dropout

题目：结合来源「第 5 章 正则化、初始化与泛化」，完成关于「Dropout」的分析：它解决什么问题、核心机制是什么、如何在实验或代码中验证？

答案：Dropout 在训练时随机关闭部分神经元，迫使网络不能过度依赖某些特征组合，起到正则化作用。 本题可使用公式 `A_drop = A * mask / keep_prob` 辅助解释。

解析：本题的来源证据是：本题主要依据课程地图、章节讲义和实验任务综合作答。。能说明训练阶段和推理阶段 Dropout 行为不同。 正确答案还应从「第 5 章 正则化、初始化与泛化」中提取一个证据点，例如标题层级、公式、结构、代码目的或实验现象。

常见错误：在验证或测试阶段仍然启用 Dropout，导致结果随机波动。

## 6. 实验分析题：BatchNorm

题目：结合来源「超参数调试处理」（标题线索：超参数调试处理；重要程度排序；调参技巧；选择合适的范围），完成关于「BatchNorm」的分析：它解决什么问题、核心机制是什么、如何在实验或代码中验证？

答案：BatchNorm 在小批量内标准化中间激活，并学习缩放和平移参数，可以稳定训练、允许更大学习率，并在 CNN 中常和卷积层配合使用。 本题可使用公式 `z_hat = (z - mu) / sqrt(sigma^2 + eps)` 辅助解释。

解析：本题的来源证据是：超参数调试、Batch 正则化和程序框架 ## 超参数调试处理 ### 重要程度排序 目前已经讲到过的超参数中，重要程度依次是（仅供参考）： * **最重要**： * 学习率 α；。能区分 BatchNorm 在训练阶段使用 batch 统计量、推理阶段使用移动平均统计量。 正确答案还应从「超参数调试处理」中提取一个证据点，例如标题层级、公式、结构、代码目的或实验现象。

常见错误：把 BatchNorm 当成输入数据预处理，而不是网络内部层。

## 7. 选择题：数据增强

题目：结合来源「第 5 章 正则化、初始化与泛化」，完成关于「数据增强」的分析：它解决什么问题、核心机制是什么、如何在实验或代码中验证？

答案：数据增强通过裁剪、翻转、颜色扰动等方式扩展训练分布，尤其适合图像任务提升泛化能力。 

解析：本题的来源证据是：本题主要依据课程地图、章节讲义和实验任务综合作答。。能区分训练增强和测试预处理的不同目标。 正确答案还应从「第 5 章 正则化、初始化与泛化」中提取一个证据点，例如标题层级、公式、结构、代码目的或实验现象。

常见错误：使用改变语义的增强，例如把数字或医学图像随意翻转。

## 8. 判断题：早停

题目：结合来源「第 5 章 正则化、初始化与泛化」，完成关于「早停」的分析：它解决什么问题、核心机制是什么、如何在实验或代码中验证？

答案：早停根据验证集表现停止训练，避免模型在训练集上继续拟合噪声，是一种实践中的正则化策略。 

解析：本题的来源证据是：本题主要依据课程地图、章节讲义和实验任务综合作答。。能根据验证 loss 连续不下降判断何时停止并保存最佳模型。 正确答案还应从「第 5 章 正则化、初始化与泛化」中提取一个证据点，例如标题层级、公式、结构、代码目的或实验现象。

常见错误：只保存最后一轮模型，而不是验证集表现最好的模型。

## 9. 简答题：训练/验证/测试划分

题目：结合来源「数据划分：训练 / 验证 / 测试集」（标题线索：数据划分：训练 / 验证 / 测试集；建议；补充：交叉验证（cross validation）；参考资料），完成关于「训练/验证/测试划分」的分析：它解决什么问题、核心机制是什么、如何在实验或代码中验证？

答案：训练集用于拟合参数，验证集用于调模型和超参数，测试集用于最终估计泛化性能，三者职责不能混用。 本题可使用公式 `J(w,b) = \frac{1}{m}\sum_{i=1}^mL(\hat{y}^{(i)},y^{(i)})+\frac{\lambda}{2m}{||w||}^2\_2` 辅助解释。

解析：本题的来源证据是：深度学习的实用层面 ## 数据划分：训练 / 验证 / 测试集 应用深度学习是一个典型的迭代过程。。能说明为什么不能根据测试集结果反复调参。 正确答案还应从「数据划分：训练 / 验证 / 测试集」中提取一个证据点，例如标题层级、公式、结构、代码目的或实验现象。

常见错误：把验证集当测试集反复使用，导致泛化评估过于乐观。

## 10. 计算/推导题：偏差方差

题目：结合来源「数据划分：训练 / 验证 / 测试集」（标题线索：数据划分：训练 / 验证 / 测试集；建议；补充：交叉验证（cross validation）；参考资料），完成关于「偏差方差」的分析：它解决什么问题、核心机制是什么、如何在实验或代码中验证？

答案：偏差反映模型拟合能力不足，方差反映模型对训练数据过于敏感；二者帮助判断欠拟合和过拟合。 本题可使用公式 `J(w,b) = \frac{1}{m}\sum_{i=1}^mL(\hat{y}^{(i)},y^{(i)})+\frac{\lambda}{2m}{||w||}^2\_2` 辅助解释。

解析：本题的来源证据是：### 参考资料 无偏估计_百度百科 ## 模型估计：偏差 / 方差 **“偏差-方差分解”（bias-variance decomposition）**是解释学习算法泛化性能的一种重要工具。。能根据 train/dev error 判断高偏差、高方差或两者兼有。 正确答案还应从「数据划分：训练 / 验证 / 测试集」中提取一个证据点，例如标题层级、公式、结构、代码目的或实验现象。

常见错误：看到验证误差高就盲目加深模型，不先区分偏差和方差。

## 11. 代码理解题：初始化

题目：结合来源「课程2 第1周 作业题 深度神经网络的初始化 正则化 梯度检验」，完成关于「初始化」的分析：它解决什么问题、核心机制是什么、如何在实验或代码中验证？

答案：初始化决定训练开始时参数尺度和对称性，合适初始化能保持激活和梯度在合理范围内，减少训练不稳定。 

解析：本题的来源证据是：# ***笔记前言*** 1. 初始化参数： - 1.1：使用0来初始化参数。。能说明为什么全零初始化会让同层神经元学习相同特征。 正确答案还应从「课程2 第1周 作业题 深度神经网络的初始化 正则化 梯度检验」中提取一个证据点，例如标题层级、公式、结构、代码目的或实验现象。

常见错误：随意初始化权重，不观察梯度和激活是否饱和。

## 12. 实验分析题：L2 正则化

题目：结合来源「数据划分：训练 / 验证 / 测试集」（标题线索：数据划分：训练 / 验证 / 测试集；建议；补充：交叉验证（cross validation）；参考资料），完成关于「L2 正则化」的分析：它解决什么问题、核心机制是什么、如何在实验或代码中验证？

答案：L2 正则化在代价函数中惩罚较大的权重，促使模型参数更平滑，降低过拟合风险。 本题可使用公式 `J_reg = J + lambda/(2m) ||W||_2^2` 辅助解释。

解析：本题的来源证据是：本题主要依据课程地图、章节讲义和实验任务综合作答。。能说明正则化强度 lambda 增大时训练误差和验证误差可能如何变化。 正确答案还应从「数据划分：训练 / 验证 / 测试集」中提取一个证据点，例如标题层级、公式、结构、代码目的或实验现象。

常见错误：把 L2 当作一定提升准确率的技巧，不结合过拟合程度选择。
