# 第 10 章 序列模型：RNN、GRU 与 LSTM

## 章节定位

从序列数据、RNN、BPTT、梯度消失、GRU、LSTM 门控到 PyTorch nn.LSTM 实验。 本章不再使用概念占位模板，而是将 数学符号；循环神经网络RNN；循环神经网络RNN的实现；课程5 第1周 循环神经网络RNN；课程5 第1周 作业题a 搭建RNN网络和LSTM网络；序列模型；门控循环单元GRU；长短期记忆网络LSTM 中的课程结构、公式线索和 notebook 实验顺序整理为统一讲义。学生端看到的是重构后的课程内容；原始来源只作为内部依据和可追溯证据。

## 来源融合说明

- **数学符号**（Andrew Ng 笔记）：覆盖 数学符号、循环神经网络模型、反向传播、不同结构、语言模型。
  - 公式线索：x；x^{⟨t⟩}；t
  - 内容要点：循环序列模型 自然语言和音频都是前后相互关联的数据，对于这些序列数据需要使用**循环神经网络（Recurrent Neural Network，RNN）**来进行处理。
  - 内容要点：使用 RNN 实现的应用包括下图中所示： ## 数学符号 对于一个序列数据 $x$，用符号 $x^{⟨t⟩}$来表示这个数据中的第 $t$个元素，用 $y^{⟨t⟩}$来表示第 $t$个标签，用 $T\_x$ 和 $T\_y$来表示输入和输出的长度。
- **循环神经网络RNN**（AccumulateMore/CV Notebook）：覆盖 未提取标题层级。
  - 内容要点：# 1. 循环神经网络
- **循环神经网络RNN的实现**（AccumulateMore/CV Notebook）：覆盖 未提取标题层级。
  - 代码线索：course code example；course code example；course code example
  - 内容要点：# 1. RNN的实现(不用框架) ① 梯度裁剪$$\mathbf{g} \leftarrow \min\left(1, \frac{\theta}{\|\mathbf{g}\|}\right) \mathbf{g}$$ # 2. RNN的实现(用框架)
- **课程5 第1周 循环神经网络RNN**（AccumulateMore/CV Notebook）：覆盖 未提取标题层级。
  - 内容要点：# ***循环神经网络RNN*** # 1. 序列模型 ① 序列模型能够应用在许多领域，例如： - 语音识别 - 音乐发生器 - 情感分类 - DNA序列分析 - 机器翻译 - 视频动作识别 - 命名实体识别 ② 这些序列模型基本都属于监督式学习，但输入x和输出y不一定都是序列模型。
  - 内容要点：③ 如果都是序列模型的话，模型长度也不一定完全一致。
- **课程5 第1周 作业题a 搭建RNN网络和LSTM网络**（AccumulateMore/CV Notebook）：覆盖 未提取标题层级。
  - 代码线索：course code example；course code example；course code example
  - 内容要点：# ***搭建RNN网络和LSTM网络*** # 0. 要解决的问题 ① 循环神经网络（RNN）对于自然语言处理和其他序列任务非常有效，因为它们具有“记忆”功能。
  - 内容要点：② 它们可以一次读取一个输入$x^{\langle t \rangle}$（例如单词），并且通过隐藏层激活从一个时间步传递到下一个时间步来记住一些信息/上下文，这允许单向RNN从过去获取信息来处理后面的输入，双向RNN可以从过去和未来中获取上下文。
- **序列模型**（AccumulateMore/CV Notebook）：覆盖 未提取标题层级。
  - 代码线索：course code example；course code example；course code example
  - 内容要点：# 1. 序列模型 # 2. 序列模型 ① 使用正弦函数和一些可加性噪声来生成序列数据，时间步为1,2,...,1000 ② 将数据映射为数据对$y_t = x_t$和$\mathbf{x}_t = [x_{t-\tau}, \ldots, x_{t-1}]$ ③ 使用一个相当简单的结构：只是一个拥有两个全连接层的多层感知机 ④ 训练模型 ⑤ 模型预测下一个
- **门控循环单元GRU**（AccumulateMore/CV Notebook）：覆盖 未提取标题层级。
  - 代码线索：course code example；course code example；course code example
  - 内容要点：# 1. 门控循环单元GRU ① 相当于全连接层把X和H并在一起。
  - 内容要点：③ $R_{t}$是可以学的，它可以根据前面的信息，看到这种情况知道哪些东西pass到下面的情况，哪些东西不pass到下面的情况。
- **长短期记忆网络LSTM**（AccumulateMore/CV Notebook）：覆盖 未提取标题层级。
  - 代码线索：PyTorch LSTM example；PyTorch LSTM example；PyTorch LSTM example
  - 内容要点：# 1. 长短期记忆网络LSTM # 2. 长短期记忆网络LSTM

## 学习目标

- 能解释「序列数据」的定义、来源中的具体语境、公式或代码位置，并完成至少一个可检查任务。
- 能解释「RNN」的定义、来源中的具体语境、公式或代码位置，并完成至少一个可检查任务。
- 能解释「BPTT」的定义、来源中的具体语境、公式或代码位置，并完成至少一个可检查任务。
- 能解释「梯度消失」的定义、来源中的具体语境、公式或代码位置，并完成至少一个可检查任务。
- 能解释「GRU」的定义、来源中的具体语境、公式或代码位置，并完成至少一个可检查任务。
- 能解释「LSTM」的定义、来源中的具体语境、公式或代码位置，并完成至少一个可检查任务。
- 能解释「遗忘门」的定义、来源中的具体语境、公式或代码位置，并完成至少一个可检查任务。
- 能解释「输入门」的定义、来源中的具体语境、公式或代码位置，并完成至少一个可检查任务。

## 前置知识

学习本章前，应先确认上一章的核心对象、变量含义和实验边界。遇到公式时，不要只背符号，要能指出它在代码中对应哪一个张量、参数、损失或指标；遇到模型结构时，要能说清数据从输入到输出经过了哪些层。

## 知识结构总览

- **序列数据**：对应来源中的「课程综合材料」，学习时同时检查定义、流程、公式/代码和实验现象。
- **RNN**：对应来源中的「数学符号」，学习时同时检查定义、流程、公式/代码和实验现象。
- **BPTT**：对应来源中的「课程综合材料」，学习时同时检查定义、流程、公式/代码和实验现象。
- **梯度消失**：对应来源中的「数学符号」，学习时同时检查定义、流程、公式/代码和实验现象。
- **GRU**：对应来源中的「数学符号」，学习时同时检查定义、流程、公式/代码和实验现象。
- **LSTM**：对应来源中的「课程5 第1周 作业题a 搭建RNN网络和LSTM网络」，学习时同时检查定义、流程、公式/代码和实验现象。
- **遗忘门**：对应来源中的「课程综合材料」，学习时同时检查定义、流程、公式/代码和实验现象。
- **输入门**：对应来源中的「课程综合材料」，学习时同时检查定义、流程、公式/代码和实验现象。
- **输出门**：对应来源中的「课程综合材料」，学习时同时检查定义、流程、公式/代码和实验现象。
- **nn.LSTM**：对应来源中的「课程综合材料」，学习时同时检查定义、流程、公式/代码和实验现象。

## 1. 序列数据

**来源位置**：课程综合材料。

**重构讲解**：序列数据具有时间或位置顺序，例如文本、音频、时间序列，当前输出往往依赖前后文。

**来源要点摘录**：
- 本节作为课程组织知识点，由课程地图、章节讲义和后续实验共同支撑。

**代码或实验落点**：
- 本节重点是概念、公式或结构理解；可在章节练习中补充最小实验。

**学习检查**：能说明为什么把一句话打乱顺序会破坏语义。

**常见误区**：像处理独立样本一样处理序列，忽略时间依赖。

## 2. RNN

**来源位置**：数学符号；标题层级包括 数学符号、循环神经网络模型、反向传播、不同结构、语言模型、采样。

**公式/结构线索**：`h_t = tanh(W_h h_{t-1} + W_x x_t + b)`。

**重构讲解**：RNN 通过隐状态 h_t 把上一时间步信息传到当前时间步，适合建模序列数据，但长序列训练中容易出现梯度消失或爆炸。

**来源要点摘录**：
- 循环序列模型 自然语言和音频都是前后相互关联的数据，对于这些序列数据需要使用**循环神经网络（Recurrent Neural Network，RNN）**来进行处理。
- 使用 RNN 实现的应用包括下图中所示： ## 数学符号 对于一个序列数据 $x$，用符号 $x^{⟨t⟩}$来表示这个数据中的第 $t$个元素，用 $y^{⟨t⟩}$来表示第 $t$个标签，用 $T\_x$ 和 $T\_y$来表示输入和输出的长度。
- 为了解决这些问题，引入**循环神经网络（Recurrent Neural Network，RNN）**。

**代码或实验落点**：
- 本节重点是概念、公式或结构理解；可在章节练习中补充最小实验。

**学习检查**：能画出 RNN 按时间展开后的结构，并说明 BPTT 如何传播梯度。

**常见误区**：把 RNN 当作普通全连接网络重复使用，忽略时间步共享参数。

## 3. BPTT

**来源位置**：课程综合材料。

**重构讲解**：BPTT 是把 RNN 沿时间展开后进行反向传播，通过所有时间步把损失梯度传回共享参数。

**来源要点摘录**：
- 本节作为课程组织知识点，由课程地图、章节讲义和后续实验共同支撑。

**代码或实验落点**：
- 本节重点是概念、公式或结构理解；可在章节练习中补充最小实验。

**学习检查**：能解释为什么长序列 BPTT 容易出现梯度消失或爆炸。

**常见误区**：只画循环结构，不知道训练时会按时间展开计算梯度。

## 4. 梯度消失

**来源位置**：数学符号；标题层级包括 数学符号、循环神经网络模型、反向传播、不同结构、语言模型、采样。

**重构讲解**：梯度消失指反向传播中梯度逐层或逐时间步变得很小，导致早期层或远距离依赖难以学习。

**来源要点摘录**：
- 本节作为课程组织知识点，由课程地图、章节讲义和后续实验共同支撑。

**代码或实验落点**：
- 本节重点是概念、公式或结构理解；可在章节练习中补充最小实验。

**学习检查**：能说明 sigmoid/tanh 饱和区和长序列相乘如何加剧梯度消失。

**常见误区**：看到训练慢就直接换模型，不检查梯度和激活范围。

## 5. GRU

**来源位置**：数学符号；标题层级包括 数学符号、循环神经网络模型、反向传播、不同结构、语言模型、采样。

**重构讲解**：GRU 用更新门和重置门控制历史信息保留与候选状态写入，参数少于 LSTM，常用于序列建模的轻量门控方案。

**来源要点摘录**：
- 本节作为课程组织知识点，由课程地图、章节讲义和后续实验共同支撑。

**代码或实验落点**：
- 本节重点是概念、公式或结构理解；可在章节练习中补充最小实验。

**学习检查**：能比较 GRU 更新门与 LSTM 遗忘门/输入门的作用差异。

**常见误区**：认为 GRU 只是 LSTM 的简写版本，而不理解门结构不同。

## 6. LSTM

**来源位置**：课程5 第1周 作业题a 搭建RNN网络和LSTM网络。

**重构讲解**：LSTM 用细胞状态和输入门、遗忘门、输出门控制长期信息流，缓解普通 RNN 长期依赖学习困难。

**来源要点摘录**：
- # ***搭建RNN网络和LSTM网络*** # 0. 要解决的问题 ① 循环神经网络（RNN）对于自然语言处理和其他序列任务非常有效，因为它们具有“记忆”功能。

**代码或实验落点**：
- course code example
- course code example
- course code example

**学习检查**：能说明 c_t 和 h_t 的区别，以及三个门分别控制什么。

**常见误区**：把所有门都理解成同一种开关，不区分保留、写入和输出。

## 7. 遗忘门

**来源位置**：课程综合材料。

**重构讲解**：遗忘门控制 LSTM 细胞状态中旧信息保留多少，是处理长期记忆的重要门控。

**来源要点摘录**：
- 本节作为课程组织知识点，由课程地图、章节讲义和后续实验共同支撑。

**代码或实验落点**：
- 本节重点是概念、公式或结构理解；可在章节练习中补充最小实验。

**学习检查**：能说明 f_t 接近 0 和接近 1 时细胞状态发生什么变化。

**常见误区**：把遗忘门理解成删除整个历史，而不是按维度控制保留比例。

## 8. 输入门

**来源位置**：课程综合材料。

**重构讲解**：输入门控制当前候选信息写入细胞状态的程度，决定新信息是否进入长期记忆。

**来源要点摘录**：
- 本节作为课程组织知识点，由课程地图、章节讲义和后续实验共同支撑。

**代码或实验落点**：
- 本节重点是概念、公式或结构理解；可在章节练习中补充最小实验。

**学习检查**：能区分输入门 i_t 和候选状态 c_tilde 的角色。

**常见误区**：把输入门和输出门混淆，不能说明谁控制写入、谁控制暴露。

## 9. 输出门

**来源位置**：课程综合材料。

**重构讲解**：输出门控制细胞状态中哪些信息用于生成当前隐藏状态 h_t，影响当前时间步输出。

**来源要点摘录**：
- 本节作为课程组织知识点，由课程地图、章节讲义和后续实验共同支撑。

**代码或实验落点**：
- 本节重点是概念、公式或结构理解；可在章节练习中补充最小实验。

**学习检查**：能说明 c_t 与 h_t 的区别，以及输出门如何连接二者。

**常见误区**：认为 LSTM 只有一个状态，不区分细胞状态和隐藏状态。

## 10. nn.LSTM

**来源位置**：课程综合材料。

**重构讲解**：nn.LSTM 是 PyTorch 封装的 LSTM 层，输入输出形状与 batch_first、num_layers、hidden_size 等参数密切相关。

**来源要点摘录**：
- 本节作为课程组织知识点，由课程地图、章节讲义和后续实验共同支撑。

**代码或实验落点**：
- 本节重点是概念、公式或结构理解；可在章节练习中补充最小实验。

**学习检查**：能解释 output、h_n、c_n 三个返回值分别表示什么。

**常见误区**：只拿 output 最后一维做分类，不检查 batch_first 和序列维度。

## 教材式例子一：从来源结构到学习流程

以「数学符号」为例，先读取来源标题层级，判断它属于概念解释、模型结构、优化策略还是实验任务；再将它映射到本章的 序列数据, RNN, BPTT, 梯度消失；最后生成一条可执行学习任务：阅读主讲义、完成练习、运行或观察实验、写出错因复盘。

## 教材式例子二：从 notebook 到课程实验

AccumulateMore/CV 中的 notebook 不是直接塞给学生，而是被拆成实验目标、关键代码用途、运行步骤和调参任务。学生学习 第 10 章 序列模型：RNN、GRU 与 LSTM 时，应能把 `Dataset/DataLoader`、`nn.Module`、`loss.backward()`、`optimizer.step()` 或对应模型结构放回完整训练循环。

## 易错点与纠偏

- 只背来源标题，不知道标题下真正讨论了什么模型、公式或实验。
- 把 notebook 当成可直接展示的学习资源，而没有整理成目标、步骤、任务和评价。
- 学模型结构时不检查输入输出 shape，导致代码能抄但不能解释。
- 学优化或正则化时不看训练/验证曲线，无法判断方法是否真的改善泛化。

## 课堂讨论问题

1. 序列数据 与 RNN 在本章流程中分别解决什么问题？
2. 如果学生已经会使用工具库，但解释不清 序列数据，应该如何安排补弱？
3. 哪些内容适合做动画或流程图，哪些内容更适合做代码实验？

## 自测题

1. 围绕「序列数据」写出定义、输入输出、常见误区和一个应用场景。
2. 围绕「RNN」写出定义、输入输出、常见误区和一个应用场景。
3. 围绕「BPTT」写出定义、输入输出、常见误区和一个应用场景。
4. 围绕「梯度消失」写出定义、输入输出、常见误区和一个应用场景。
5. 围绕「GRU」写出定义、输入输出、常见误区和一个应用场景。
6. 围绕「LSTM」写出定义、输入输出、常见误区和一个应用场景。
7. 围绕「遗忘门」写出定义、输入输出、常见误区和一个应用场景。
8. 围绕「输入门」写出定义、输入输出、常见误区和一个应用场景。
9. 围绕「输出门」写出定义、输入输出、常见误区和一个应用场景。
10. 围绕「nn.LSTM」写出定义、输入输出、常见误区和一个应用场景。
11. 围绕「序列数据」写出定义、输入输出、常见误区和一个应用场景。
12. 围绕「RNN」写出定义、输入输出、常见误区和一个应用场景。

## 小结与下一步

完成本章后，应能把 序列数据, RNN, BPTT, 梯度消失 串成一条可执行的学习路径。下一步不是盲目堆资料，而是根据画像、诊断结果和当前章节位置选择主讲义、练习、实验或项目任务。
